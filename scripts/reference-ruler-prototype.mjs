import {chromium} from '../apps/web/node_modules/playwright/index.mjs';
import assert from 'node:assert/strict';
import {writeFile} from 'node:fs/promises';
const browser=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});
try{const page=await browser.newPage({viewport:{width:1920,height:1080}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:4174/content/prototypes/pages/creation.html');await page.locator('#start-story').click();await page.locator('#choose-story').click();await page.locator('#to-board').click();await page.locator('.board').waitFor();await page.locator('[data-line-tts]').first().click();assert.equal(await page.locator('#studio-dialog-title').innerText(),'配置配音');await page.locator('#close-studio').click();
for(const theme of ['light','dark','sky','noir']){await page.locator('#ui-theme').selectOption(theme);await page.screenshot({path:`evidence/reference-ruler/prototype-${theme}-board.png`});for(const kind of ['角色','场景','道具']){await page.locator(`[data-elements="${kind}"]`).click();assert.equal(await page.locator('.element-table th').count(),7);assert.equal(await page.locator('.element-table').evaluate(n=>getComputedStyle(n).display),'table');assert(await page.locator('.element-table thead').isVisible());await page.locator('#studio-dialog').screenshot({path:`evidence/reference-ruler/prototype-${theme}-${kind}.png`});await page.locator('#close-studio').click();}}
assert.deepEqual(errors,[]);await writeFile('evidence/reference-ruler/prototype.json',JSON.stringify({themes:4,windows:12,errors,providerCalls:0},null,2));console.log('Prototype: 4 themes / 12 entity windows / microphone dialog passed');}finally{await browser.close()}
