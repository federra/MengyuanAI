# 生产 UI 对齐最新 PRD

用户已明确授权先审查同尺寸原型/生产截图，再实施必要前端改造；原型还原度是验收标准。基线7f01029；不改后端、API、任务/版本契约，不新增M2/M3/TXT能力，不更改真实项目数据。用户现有PRD/V11工作区改动保留。

## 基准与差距

AGENTS、PRD01/03/04/05/06/07、R1/R2与manifest已读取。manifest采用1440×900。原型入口为4173/content/prototypes/pages/；实际生产5180。当前截图在.local/ui-alignment/reference和before，已逐图查看；读取中截图不作为完整页面基准，已补拍加载完成图。真实M1项目11镜、长文和失败记录用于生产对照。

1. 全局：原型全宽66px品牌顶栏、下方188px侧栏；生产额外英文/泛标题和资料块下推内容。恢复流程条→28px阶段标题→工作区层级，资料收为紧凑辅助入口。
2. 项目：恢复四格统计、左侧记录（内部两列）+右侧290px创建区。继续使用真实统计和现有创建接口。
3. 创意/故事/剧本：恢复200px候选/弹性正文/290px导演比例，浅输入底、正文1.8行高、故事350px/剧本约400px以上编辑区、纵向340px主操作。元信息/历史/写作要求按需展开。
4. 分镜：宽表局部滚动、台词卡、四类引用窗口、提示词足够列宽，右导演独立滚动。保留全部稳定ID、编辑、删除排序、过期/保存/确认语义，未接入媒体不得模拟。
5. 设置：顶栏直达三Tab模态面板，左文字树/右表单双列，保存固定可达；系统设置页保留原型四配置入口及输出默认配置。生产API密钥区继续真实保存/测试，空密码不回填。

最新V11增加noir第四主题，本轮与原三主题一起同步；原主题存储键不变，字体按Satoshi/系统/PingFang回退。R1早期“四项入口”和正式PRD05最新面板三Tab不冲突：系统页入口与顶栏弹窗分别实现。原型演示媒体/画面不复制到生产，不用无来源图装饰候选或封面。

## 实施与验收

- 根代理维护公共style.css、StoryWorkbench/StageWorkbench视觉结构和截图对照；独立前端子任务修改main.tsx/Configuration/ModelCredentials壳与配置布局，限定文件不互相覆盖。必要共享class约定同步。最终独立审查不只看测试，需检查截图。
- 保留已有21页面用例；为新模态关闭/焦点、四主题与编辑不丢失、布局溢出等有行为风险变更新增针对性用例。测试允许独立替身验证加载/失败，生产截图必须真项目且明确区分。
- 逐页同尺寸对照reference/after；检查浅/深/晴空蓝/曜夜鎏金，长文、多镜、空、加载、失败，候选/导演收起/悬浮、设置三Tab与文字树。明显差距修复后重拍。截图不等同无障碍合规，另查键盘焦点与基本对比度。
- 构建/契约/后端回归按影响范围，业务链既有证据保留；本轮无需新付费模型调用。生成可本地查看的逐页截图对照报告，提交明确工程文件，用户实际试用后才形成主观UI认可。

## 本轮工程完成记录

上述范围已实施，22 项前端回归及构建/静态检查通过。7 组原型与实页对照、28 张四主题生产截图和真实失败/空状态在 `.local/ui-alignment/`，记录见 `docs/engineering/ui-prd-alignment.md`。独立复核发现的模型底栏遮挡、三Tab和输出设置草稿卸载均已修复。用户主观视觉认可待实际试用，本轮不继续 M2–M3。
