# 参考图标尺增量 · 2026-09-10

当前基线为 codex/v12-v13-ui-alignment 的既有未提交实体工作流增量；保留先前全部代码、验收记录、素材与历史。本轮不提交、合并、推送或公网部署。

## 差异与最小实施顺序

已具备：PG实体版本、参考输入/输出独立存储、整表事务保存、稳定绑定、确认/过期、图片持久任务、资产快照和M0–M3链。
需修改：主内容1700px最大宽度、分镜台词/TTS操作条、2×2引用框、提示词附属字段、190px选择栏式实体管理。
缺失：字段显隐、声音资产资源、同一次分镜模型响应解析全剧本目录及显式关联。
实施顺序：正式PRD07/08及R1/R2/R4/R5/U规则和原型先更新 → 复用版本资源扩展voice类型与任务冻结目录契约 → 前端紧凑分镜/整表/弹窗 → 隔离回归、四主题和真实页面只读 → 本地后端重新载入 → 用户视觉与真实解析试用。

## 数据与恢复

本轮不新增表/列，不需要数据库迁移。声音资产使用configuration_resources/versions，kind=voice、stage=voice、content为合法供应商音色ID，选择后作为项目实体voice值保存；不改动旧角色音色。目录使用新任务snapshot.entity_catalog_contract=1，GeneratedBoard包含catalog与shotEntities；旧快照继续BoardBody v2。校验通过后在finish_job事务内创建实体、稳定镜头引用和台词绑定，与分镜一起发布；过期生成不激活目录，完整响应保存在任务结果供追溯。新响应不得覆盖原实体/人工版本。已有项目不额外付费回填目录。

布局和字段显隐无需迁移；显隐只保存在当前浏览器项目偏好，不是正式内容存储。时长继承方式已向用户提出精确澄清，当前先保留真实各镜时长，不擅自改视频或剪辑。

重新载入后端前检查无活动任务；若需要停止当前服务，先请求具体停写/备份/重启授权。已存在恢复基线：.local/backups/20260910T082848960025Z（13项目36文件）；随后新增无模型调用的元素验收项目尚不在该历史备份。新备份必须在停写后创建并演练，不能把旧13项目备份宣称当前14项目完整备份。
