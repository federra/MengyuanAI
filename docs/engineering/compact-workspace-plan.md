# 侧栏与分镜紧凑交互增量

基线aa9cf0c，codex/v12-v13-ui-alignment，2026-09-10。用户要求先改PRD原型再改开发系统；原型共享导航、分镜与元素管理代码先行落地后开始生产代码。本文件为实施范围，不代表已验收。

## 实际差异与最小顺序

1. 已实现：五模块导航、六列分镜、三类全片元素统一版本存储、真实图片任务/上传/确认、逐句及批量TTS、站位任务、项目规格、整表JSON事务导入。
2. 需修改：导航符号和固定宽度；工具入口跨标题/表内/页底分散；台词、四类引用与绑定详情展开过多；表头三项用词与最新要求不符；元素图片能力藏在编辑区。
3. 缺失：持久可折叠图标栏、集中七按钮工具栏、批量操作先预览后提交的配置窗口、明确元素图片操作入口。确认图片下拉应过滤未确认/过期图，保留已有引用状态。
4. 顺序：PRD07/08+R1/R2/R5及原型 → 导航与几何 → 工具栏/批量/规格弹窗 → 实体及引用管理 → 隔离测试/四主题/键盘缩放 → 本地检查点。

## 约束

不改数据库schema或公共AI协议。所有新入口复用原API的projectId/entityId/revision、确认、源版本、幂等及过期检查；开窗不发付费调用。批量确认仅提交当前有效候选，已有任务及有效结果跳过，局部失败保留结果和重试入口。UI偏好可本地存储，正式内容继续PostgreSQL。关闭/切换保留草稿，导航折叠不卸载当前工作台。

## 验收

导航展开188/折叠68，五入口可辨识/键盘可达/持久偏好；七按钮顺序与弹窗；列名、14px正文及紧凑尺寸；三类元素编辑/生成请求ID和版本、上传确认、稳定引用替换；批量预览无请求/重复点击/失败保留；规格保存与JSON导入原机制回归。公共导航涉及九页四主题，200%缩放、对话框焦点、草稿保留；不以替身状态冒充供应商验收，费用0目标，U08由用户验收。
