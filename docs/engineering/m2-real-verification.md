# M2 真实媒体验收（进行中）

用户已批准上限：总30元；图片最多10张；配音最多10次且累计1000字符；视频最多5次、累计25秒。批准覆盖计划内重试，不自动重发未知受理。密钥仅页面录入，状态查询不读取或回显密钥。

项目：`22589f8d-e8d6-4503-a997-31afc5f6e446`，名称「M2 真实媒体验收 · 回家吃饭 · 三镜」，16:9、720P。输入由已验收M1故事/剧本人工复制、选取三镜并缩为每镜5秒，内容origin=manual、新项目新稳定ID；不是新文本模型验收，原M1未修改。故事初始化复用领域服务，其余分镜保存/确认、元素/引用和生成均通过正式API。

## 计费依据与记录

- Seedream4.5：0.25元/张；Seedance2.0 Mini无视频输入：23元/百万tokens。[火山官方产品页](https://www.volcengine.com/product/doubao)
- MiniMax speech-2.8-hd：3.5元/万字符。[官方按量计费](https://platform.minimaxi.com/docs/guides/pricing-paygo)
- Seedance token估算公式为（输入视频秒数+输出视频秒数）×宽×高×帧率/1024，实际以usage为准。[BytePlus同系列官方计费说明](https://docs.byteplus.com/docs/ModelArk/1099320)。本次只输入图片、5秒1280×720×24fps按108000tokens估算，即约2.484元/镜；每次预留5元，包括在途与未知受理。不以预留金额冒充供应商账单。
- 本地持久调用台账 `.local/m2-real/state.json`（不入Git，不含密钥）；提交前保存幂等键和预留费用，防止响应丢失后重复调用。

## 已发生事实

三类凭据均stored且configured=true。MiniMax最终按用户提供并经官方语音文档核对的`https://api.minimax.cn/v1`配置，凭据revision2、绑定revision4；之前否定.cn的指导错误已更正。

已提交8次真实Seedream：小杰、父亲、饭厅、信封及信封修正版、三张站位图。前5次已成功并保存真实JPEG；小杰/父亲三视图、饭厅及完整信封已目视核验。首版信封为过近特写，保留历史未采用；不是工程失败或供应商失败。人物图可作外观参考，但不是全身三视图。站位图正在验收。

首段真实MiniMax配音（10字，job `a0811675-f8e7-4650-b3bc-b80516d07427`）返回`provider_invalid_response`并记录unknown，无音频结果、未自动重发；保留0.0035元预留，实际是否计费待核实。正在补充只包含固定错误码/数值业务码的安全诊断，禁止记录密钥、响应正文或音频数据。

尚未完成：真实视频、逐段配音、真实跨会话UI重载、失败恢复/中断恢复、上游失效及跨项目隔离、最终媒体备份与恢复。工程101后端/28页面通过不替代本记录。

更新：8张真实JPEG均已成功，7张已采用、首版信封保留未采用。全部8张经真实HTTP重载并与文件表SHA256一致；隔离对照项目逐一跨项目读取与确认均404（16项）。三张站位图已目视核验并绑定分镜v3。

TTS安全诊断增量通过105项后端回归、契约/构建。重启后提交第二段独立台词16字，job `7d44d8bb-fb04-4587-938c-f4d41e3f7485`返回业务码1008（官方定义余额不足）；无音频结果，暂停后续语音请求，等待用户补可用额度。首段仍保持unknown，未猜测其历史业务码。

视频顺序任务已创建：首镜`5b5dc146-f921-4b5a-ae20-935dd34eb2fc`提交被同步拒绝，无受理ID，状态failed；第二镜`1fa3ac2d-70e1-4421-ad23-dc9d52542bcb`和第三镜`cc36efdd-e4ec-4e36-9e79-3d0100838722`保持waiting_dependency且无受理ID，证明失败未跳镜。正在补4xx安全诊断，未重发首镜。顺序链预留15元，累计预留17.0091元；非实际账单。

## 充值后配音验收更新（2026-09-09）

用户确认语音额度充值后，另两句分别生成成功；用户随后明确批准前两句各重试一次，两次也成功。累计6次语音请求、73个输入字符（含两次失败及重试），得到4段真实MP3：

| 台词 | 成功任务 | 文件 | 解码时长 |
|---|---|---|---|
| 爸，这封信是你寄的？ | 425a6c5f-d7c0-4d58-8148-7262355f904a | 2e0d87de-17c9-5ad1-afa7-6d4bb4865199 | 2.80秒 |
| 地址是我写的，里面那句话却不是。 | 4baaa4b3-8a65-4027-8c10-3155dbcc054c | 92bfb11f-e741-52d7-9d5e-b01b0a99188e | 3.30秒 |
| 爸，对不起，我该多回家。 | 9faa7d35-324b-479e-a474-1faf1c8216b9 | 77724385-55ec-518a-b044-824873a8eaf1 | 3.49秒 |
| 菜都凉了，去热热。 | 4a339e23-ea20-41c7-a327-7583e099d125 | b6ea2c71-e650-553e-8f35-1336152601ff | 2.17秒 |

四文件经正式HTTP读取与本地SHA256一致，跨项目读取均404；独立浏览器会话打开真实分镜后，4个audio均readyState=4、无解码错误，18个图片实例均成功加载。尚未计入人工逐句听感验收。供应商usage_characters可能高于输入字符，台账同时记录两者并据usage调整费用预留；不是以输入字数冒充实际计费量。

上游失效实测：专用M2项目分镜v4修改第一句，只有对应音频标记过期，其余3段保持有效，原音频仍可读取；v5恢复原文并重新确认，4段恢复有效。全过程无新增供应商请求，保留v3/v4/v5历史，原M1项目未修改。

视频首镜重试任务`ace3a62a-60c2-407c-86a0-2fa4741a7ab1`仍同步失败`provider_http_400`、无外部任务ID；第二/三镜继续等待依赖。已分配4个视频任务（包含尚未发送的2镜），按20秒计入预算；剩余1个视频任务/5秒额度不盲目消耗。当前总预留22.07645元（含失败及待发送），不等于已消费账单。已核对请求主要字段符合官方公开限制，尚不能确定此次400的具体原因；旧响应被安全诊断丢弃，无法追溯原错误正文，待控制台错误线索。

工程最近完整回归111项后端及28项页面通过。M2仍未通过：真实三镜视频与前镜尾帧链、真实视频中断恢复和中间镜头失败重试、最终媒体备份恢复仍待完成；替身验证不能替代这些事实。

最终本轮工程回归：116项后端通过（2个既有弃用警告）、OpenAPI契约一致、前端构建与make check通过。诊断修复已优雅重启加载；重启后12个真实媒体结果（8图+4音频）保留、4段音频HTTP哈希不变、两镜等待依赖且无外部ID，未新增任务。这是服务重启后的持久恢复证据，不是供应商生成中断恢复或备份恢复证据。

## HTTP400诊断补充

用户反馈控制台未找到调用记录、余额未变化，官方建议检查响应Code。代码复现证明只解析error.code会漏掉Code/Error.Code/ResponseMetadata.Error.Code，同时缺少OutputVideoSensitiveContentDetected分类。已先通过4个失败用例复现，再补齐解析（含ResponseMetaData变体）、固定审核分类，并仅在服务日志保留格式受限的机器错误码；不记录响应Message、响应正文、凭据、请求图片。此为诊断缺口修复，不能据此认定前次400的业务原因或未收费。

实测定位：最后一次额度内首镜重试`4717d39e-efa6-4a64-b00f-adc5aa83a998`于2026-09-09 06:55:13返回HTTP400，安全日志取得真实Code=`InputImageSensitiveContentDetected.PrivacyInformation`。无视频任务ID、无视频结果。这个码来自此次真实响应，不是官方举例的OutputVideoSensitiveContentDetected，也不追溯替换前两次丢失的响应事实。当前确认是输入图片隐私审核拒绝；未定位具体参考图，不能声称图片包含真实身份或具体隐私信息。

已补精确错误分类及界面提示。最初诊断任务的历史error为provider_http_400，台账另记精确供应商Code。累计5个视频任务配额（3次首镜实际请求+2个未发送的依赖镜头），25秒名义配额已满，费用总预留27.07645元，不是已消费账单，不再自动重试。

后续官方接入方向：核对账号能否使用私域虚拟人像入库并通过asset://引用；当前适配器只接入普通图片，尚未实现该素材协议及权限流程。官方[内容安全机制](https://www.volcengine.com/activity/Seedance2-0-security?infrom=100001.100.140)说明虚拟人像入库机制，[创作权益说明](https://www.volcengine.com/docs/82379/2377608?lang=zh)涉及企业资格及素材API权益；不能推定用户具备资格，不替用户购买。已询问账号权限/官方接入说明。若账户不支持，则需重新选择符合写实参考图需求的供应商，或由用户决定调整验收素材风格；不能偷偷删掉人物参考图宣称原链路已通过。

用户随后确认已有虚拟人像权限，授权继续核对接入。已核对官方独立AK/SK鉴权、资源项目一致性、逐文件审核Active和asset://用法；待确认TOS桶/地域/资源项目后实施，具体见[m2-ark-assets-integration.md](m2-ark-assets-integration.md)。不将权限口头确认当作接口已实测。最新诊断代码125项完整后端回归、28项页面回归、构建/契约/静态检查通过，独立只读复审无阻断；额外3个日志脱敏用例加入后媒体供应商测试44项通过。未重复全套测试计数，不宣称完整128项已跑。

用户已保存素材AK/SK v1；真实HeadBucket成功，ListAssetGroups首查因漏Filter失败，用户明确批准补查后成功。合计1次TOS读取+2次方舟查询，合并批准上限1元；未新增图片上传、素材入库或视频调用。保存与读取权限已验证，自动可信素材链仍未完成。


## 单图真实上传与创建检查（2026-09-09）

用户批准1张已有角色图≤10MB、上传1次/创建素材组1次/创建素材1次/查询10次，新增上限1元，失败即停、不生成视频。实际选择小杰已确认Seedream角色图，文件e67861da-20cf-558a-a193-1107c1e81277，640067字节；SHA256=eddf2f36897c7b9016a1c2de605b9fc82c9c97fe805a528fc817b8ad57d315fd。已在私有桶mengyuanaibucket完成上传（先对象HEAD，再禁止覆盖PUT，附SHA256校验），对象键m2-trusted/22589f8d-e8d6-4503-a997-31afc5f6e446/eddf2f36897c7b9016a1c2de605b9fc82c9c97fe805a528fc817b8ad57d315fd.jpg。

随后01:22:30 UTC创建素材组返回HTTP403，未取得GroupID，按失败即停停止；没有CreateAsset/GetAsset或视频请求。台账.local/m2-real/asset-single-1.json保留上传成功与创建失败，脚本拒绝失败后重跑。未取得实际账单，1元为批准上限而非已消费费用；上传对象仍保留，未额外删除。

暴露出诊断遗漏：transport对非200先抛固定HTTP码，未保存响应业务Code，因此此次403不能定位到具体IAM/资源项目/服务权益，不能直接声称账号无私域权限。已通过失败测试复现并修复：仅保留长度/字符受限且不含AK/SK的ResponseMetadata.Error.Code，不输出Message、正文或签名URL。15项传输层隔离测试和make check通过，未为取错误码重复外部请求。真实创建仍未通过，下一次创建需用户另行批准，优先复用已上传文件。


### 素材组额外重试授权与明确错误（2026-09-09）

用户随后新增批准最多3次创建素材组，合并费用仍在此次1元上限内。仅执行其中第1次，2026-09-09 01:30:12 UTC（北京时间09:30:12），HTTP403，安全业务Code=`SubscriptionRequired`；没有GroupID。剩余2次未用，不原样盲重试。独立台账.local/m2-real/group-retry-1.json保留次数与错误，未知/成功状态拒绝重发。没有再次上传、CreateAsset或生成视频。

该码指向服务订阅/开通状态，但尚不能断言一定需要新购买：用户此前确认有权限，需核对当前AK所属账号是否已生效所需权益及首次创建授权。官方CreateAssetGroup文档要求首次在控制台签署授权函，私域素材库指南说明全部功能需要高级创作权益。由用户或官方核对，代理不代签/购买；已有TOS成功对象保留可复用。待账号状态核实后，可使用剩余2次授权，无需重复申请同一调用许可。


## 最新验收素材决策：非真人形象（2026-09-09）

用户明确后续测试先统一使用非真人图片。为避免把AI写实人物误当成免审核素材，本次采用明确非人形的二维卡通松鼠与刺猬，无人脸、无衣服、无人物照片或旧写实人物参考。原Seedream人物图本为AI生成，不能记成真实人物照片；非真人路线是否通过仍以真实供应商结果为准，不承诺必然免审核。

暂停素材组路线与剩余2次创建授权，不自动购买高级权益；既有AK/SK配置、上传对象和403/SubscriptionRequired历史保留。恢复普通参考图适配器，后续镜头仍必须使用前镜真实视频尾帧，不用站位图冒充。新项目8916ef02-2715-468f-86fa-c5dfc84673a8（M2 真实媒体验收 · 卡通小动物送果子 · 三镜）已在本地建立，四类元素为松鼠/刺猬、森林树洞前、红果子；仅手工验收输入，无模型调用。3镜各5秒，第一镜2角色2句、其余各1句，共4句，覆盖两角色多台词验收。输入台账.local/m2-nonhuman/state.json。

拟新增付费上限25元：图像8次（4元素+3站位+1失败/质量重试），TTS5次且输入总字符≤200（4句+1重试），视频4次且总请求时长≤20秒（3镜+1重试）。每次提交前预留，失败/未知也占次数，未知受理不自动重发；原人物项目费用独立保留，不能视为退还或挪用。费用为上限/预算而非实际账单；正式调用前核对当前配置与单价，若估算不能满足则停。当前等待用户批准新增付费方案，未生图/配音/视频。

通过后仅证明非真人形象的M2媒体链，写实人物入库路线未验证。M2其余保存重载、失败重试、中断恢复、失效传播、跨项目隔离要求不变，M3不纳入。


### 最新预算覆盖：三镜、整轮≤5元、最多两句（2026-09-09）

用户否决25元方案，明确整轮≤5元且仍满足三镜；现已授权该范围，无需重复请求同一预算许可。手工验收计划缩为3镜各4秒（请求总时长12秒），首镜两句“送给你。”“一起吃！”，后两镜无台词。新项目尚无供应商调用。

预算闸门：按720P/1280×720/24fps及23元/百万tokens估算，三镜仅视频5.9616元，已超过整轮上限。此前读取的官方创建视频文档列2026-08-07至10-07 Mini刊例价四折活动，按此三镜视频约2.38464元；本轮公开标价仍为原价，当前账号实际折扣未核实，因此不能先收费后核价。保留全片元素/四类参考/前镜真实尾帧要求，不静默减少为单镜、不拆轮绕过5元。确认实际费率或实现经验证的更低成本规格后再提交，旧25元/8图/4视频计划作废。


### 五元方案已获执行授权（2026-09-09）

用户确认Mini四折活动价，价格闸门解除。实际执行上限：7张图（4元素+3站位）预留1.75元，2句配音各预留0.02元，3镜各4秒/720P各预留1元，合计4.79元，整轮不得超过5元；无新增素材组/TOS调用。失败或未知不自动增加生成次数。实际金额仍以供应商账单为准，不把预算当账单。

手工验收故事/剧本/分镜已通过现有版本机制保存于非真人项目；四类卡通元素图真实生成成功，站位图生成中。两张角色图为动物卡通，模型未严格遵从四足姿态（呈直立三视图），但无写实人脸，不记成真实人像。后续只验证卡通路线，不追溯改写旧写实人物失败。


## 非真人三镜真实链检查点（2026-09-09）

新项目8916ef02-2715-468f-86fa-c5dfc84673a8本轮7张Seedream图、2次MiniMax配音（输入8字含标点，计费字符共14）、3次Seedance Mini视频全部成功，无额外重试/TOS/素材组调用。视频各请求4秒，实际各4.041667秒，总12.125001秒，1280×720/H264；两段独立MP3为1.3秒和1.149406秒，未进入M3音轨合成。

按真实usage与用户确认的四折价估算：图片1.75元，视频261900tokens×23/百万×0.4=2.40948元，配音0.0049元，合计4.16438元。整轮预算预留4.79元、硬上限5元；未核对供应商账单，不将估算写成扣款事实。

首镜cgt-20260909095813-fvr4d受理后重启API/Worker，以同一ID恢复并成功落库；第二镜cgt-20260909100050-5czbr引用首镜结果与实际提取尾帧，第三镜cgt-20260909100322-sqc9l引用第二镜结果与尾帧。15个文件（7图/2音频/3视频/3尾帧）HTTP与本地SHA256一致，旧项目读取全部404。首镜提示词修改后三镜全过期，恢复原文并确认后12个结果有效，最终分镜revision5，修改历史保留。

真实页面三段视频及两段音频readyState=4、无解码错误。刷新回到项目库后重新打开项目，分镜阶段与媒体恢复。首次浏览器脚本误假设刷新仍在项目内而定位超时，按实际导航修正后通过。证据台账：.local/m2-nonhuman/state.json、verification.json、before-restart.json、after-restart.json、app-project.png。

内容边界：第三张站位图有两颗果子；补充一颗果子的手工脚本因错误Python解释器未执行，视频仍使用原冻结快照，没有伪造该提示生效。三段视频的真实尾帧最终均保持一颗果子，但未做完整逐帧美术与叙事质量验收。角色为直立卡通动物，未严格遵循四足姿态。

本轮三镜真实顺序链、已受理任务重启恢复、保存重载、失效传播、隔离通过。M2尚未整体关闭：中镜真实失败后恢复且不重做前镜未专门演练，不能以工程替身和旧写实首镜失败链替代；最终媒体停写备份受旧项目两个待依赖任务影响，不能用旧备份代替；最终分支复审仍待收尾。当前不追加付费请求、不进入M3、不合并或推送。

镜1：job=95f11beb-a072-43b5-89a7-c61b06e57cbd；视频=6d9007f7-66af-54f9-a0bd-3dbad08f22a4；尾帧=18bb0025-83c9-59ca-9674-24d1e2a1c11d。

镜2：job=985b65db-2b3a-45c9-99dc-36c0e7808f98；视频=2a588e29-12f4-5a20-91f8-845c6227712d；尾帧=2c66acf7-8000-5b70-8cc3-085f9412bd32。

镜3：job=cd4c916a-b678-44f8-8b76-3e24cf4efecc；视频=7b2c35ce-779d-5143-84cd-3df039c0883f；尾帧=d9d4c7a6-9209-58e9-8cf9-7fc1743e8f09。

## 2026-09-09 提交、备份和服务器迁移补充

用户随后明确授权提交、合并 main、推送 GitHub 和服务器部署，覆盖上一节不合并/推送的限制。旧写实项目两个从未提交供应商的待依赖任务已取消并保留历史；停写备份 `20260909T025013260003Z` 及隔离恢复核对通过（10 项目、29 文件）。最终提交前 147 后端 / 29 页面回归通过，独立代码复审无阻断，提交 `43af009` 已合并推送。服务器迁移与公网验证见 [服务器部署记录](server-deployment.md)。中镜真实失败恢复专项演练仍未完成，不追加付费请求，不宣称 M2 所有验收关闭，不进入 M3。

## 2026-09-09 M2 最终关闭补充

用户批准新增3次视频/12秒/上限5元的真实故障演练。第二镜真实视频已下载后注入一次本地尾帧失败，第三镜确实未提交；真实页面重试复用原外部ID和暂存视频，第一镜完整记录不变，第三镜随后成功。三视频共12.125001秒/261900tokens，按已确认折扣估算2.40948元（未核账单）。正常环境播放/重载、六个新文件哈希与隔离、10项目35文件停写备份/隔离恢复及独立最终复核通过。R5 M2退出条件现已完成；详见 [最终验收](m2-final-verification.md)。该结论覆盖上文“中镜演练未完成”，不进入M3、不宣称UI或完整成片已验收。新增结果仅在本地，未推送/合并/部署。
