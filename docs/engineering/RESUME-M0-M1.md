# M0/M1 关机检查点 · 2026-09-08

用户明确要求现在暂停，不新增功能或长任务；等待重新开机后明确命令再继续。

## 已完成与验证

- 分支 `feat/m1-story-loop`，M0标签 `m0-baseline-20260908` 指向cf2d2c6，未移动。工程截至d590ef0已提交，后续本检查点文档另有提交。
- 配置底座、内容完整文本链、生产工作台及5项界面审查修复已完成。后端49项、浏览器15项、契约/构建/静态检查通过；独立任务复审Approved。
- 真实HTTP API/PG/Redis/Worker连接明确标识的模型替身：首轮19调用含PNG引用，界面修复后18调用重跑及精确原故事查询通过。两轮报告在`.local/m1-integration/report-before-ui-fixes.json`与`report.json`。
- 原生库已升至20260908_m1_content，2项目/1媒体/1创意及版本/1历史任务保留。HTTP媒体SHA256与创意读取通过；无密钥生成422且未新增任务。原生页面加载后正文、导航及零pageerror通过。
- 升级前后备份`.local/backups/20260908T104642732412Z`、`.local/backups/20260908T120606312297Z`均隔离恢复通过（2项目/1文件）。

## 未完成：仅剩最终审查两项恢复修复及复验

最终整分支审查为Changes requested，详见`docs/engineering/m0-m1-final-review.md`；尚未派发或开始这轮修复。

1. `StageWorkbench.generateStage`在明确409拒绝后仍永久保存旧target_revision，后续请求反复409。要区分明确拒绝与响应未知：前者允许重新取当前基准提交，后者保留原body/key，不能破坏已通过的丢响应恢复。
2. 重新生成上游后，阶段草稿缓存source未随“读取最新基准/载入为草稿”更新，导致后续保存持续来源冲突。需显式、保留草稿的来源一致恢复；剧本和分镜均补测试，分镜body.scriptId不得静默改标历史来源，后端严格校验保留。

下一步：读AGENTS.md、本记录与最终审查；检查Git状态及现有PRD更新；一次有界修复处理以上两项（同一实现者/单修复波），先补失败用例，保留原15项回归，运行受影响测试/构建；只复审修复范围。无新功能、无M2/M3扩展。

## 重启与现场

本轮隔离模型服务及临时库已清理；本轮原生API/Worker/dispatcher/Vite已正常停止，专用Redis已保存退出、PostgreSQL已正常关闭。没有活动任务。重新启动可执行`make dev`（依赖已安装且迁移已完成，命令会启动专用数据库和Redis）；不要读取.env或输出密钥。

用户确认DEEPSEEK_API_KEY尚未注入，本轮仅工程与替身验收；真实DeepSeek质量、耗时/费用及整个M1真实供应商退出验收仍待未来注入。不要再次要求当前注入。

用户的AGENTS/PRD/Demo修改保留在工作区，未混入工程提交；本轮追加的PRD工程状态也留在同一工作区。暂停差异副本与未跟踪文档将保存到`.local/checkpoints/20260908-pause/`。不要reset、clean、整体stage或覆盖这些改动。

SDD执行记录仍保留`.superpowers/sdd/m0-m1-current-plan/`，未完成前不得删除。最终收尾前两项既有判断必须向用户说明：停写后允许原样备份unknown（误判活动状态会造成不一致备份）；R4补P29故事质检（若不需要可撤销新增场景）。没有其他待裁决项。不要沿用旧exec session编号，关机后需重新启动服务。
