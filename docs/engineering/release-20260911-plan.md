# 2026-09-11 当前版本发布方案

用户明确授权提交当前集成版本、合并main、推送GitHub并覆盖既有服务器应用。集成基线codex/v12-v13-ui-alignment，主工作区独立PRD草稿不覆盖、不混入本次生产版本。

目标121.199.40.214原HTTPS入口；保留Nginx认证、应用账号、服务器项目、媒体和凭据。不将本地数据库覆盖服务器，不发起付费模型调用。

发布前完整隔离后端/前端测试、静态检查、契约与构建、PRD校验；归档当前代码和验收记录，main快进合并后推送，不强推。服务器建立独立release并安装锁定依赖及中文字体，再停写备份数据库/媒体/私有配置；从备份恢复到隔离数据库演练M2→M3→分镜导入→实体工作流三次升级，核对旧列行摘要不变、媒体哈希不变。演练成功才升级服务器正式库、补风格种子、切换current、启动服务。

迁移失败在开放写入前恢复备份并切回旧release；成功后保留备份与旧release。上线核对HTTPS认证、未认证拒绝、页面资源、健康、项目/素材、导出依赖与任务状态，不以部署成功代替最终UI或用户全链验收。

发布前验证完成：242后端、110页面全量通过，OpenAPI一致、构建、make check及PRD validate通过。旧页面测试已同步无二次确认及提示词链接预览/编辑入口，保留原目标、草稿和版本恢复断言；未改生产行为以迎合测试。仓库候选文本扫描未发现密钥特征，生成媒体和机器运行文件不纳入提交。
