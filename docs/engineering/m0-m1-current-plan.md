# M0/M1 最新 PRD 增量实施计划 · 2026-09-08

依据：AGENTS.md、PRD 01/03–08、R4、R5（含 V8/V9/V10）。当前工程基线 b8d0c82；M0 标签 m0-baseline-20260908 保留。现有用户 PRD/Demo 修改不得覆盖或混入工程提交。只扩展现有 PostgreSQL、版本、持久任务和 React 工程；不重写 Demo，不实现 M2 媒体或 M3 导出。真实 DeepSeek 密钥尚未注入，替身测试不能等同真实模型验收。

### Task 1: 配置和项目底座
实施 M0 所需 V8/V9/V10 增量后端及测试。阅读 PRD 04/05/08 和 R5 最新增量。新增不可变资源版本（skill/prompt/style）、方法绑定、场景模板绑定、模型类别默认/环节覆盖、系统输出默认与项目规格版本；资源库与设置使用同一服务。项目创建选画幅分辨率、旧数据只补缺失记录来源；乐观锁冲突，稳定 ID，同名同类同阶段去重，Skill MD/TXT 128KB UTF8 无执行。具名方法种子与场景模板种子可用，DeepSeek 从现有环境配置迁移为文本默认，不把 Demo 名称当模型路由。提供 resolve(db,project,key,stage) 配置快照接口供任务调用，模型/模板/方法/风格和规格冻结，无密钥值。项目类型新增、筛选排序及真实计数。只改配置相关后端、迁移、tests；不改 creation 模块及前端。迁移独立文件，模型可放新 config_models.py（Base 元数据需加载）。契约后续统一生成。测试独立且验证版本/继承/覆盖/冲突/迁移，不提交用户文档。

### Task 2: M1 内容主链后端
复用 ContentItem/Version 与 Job/Outbox；实现创意助手、具名方法故事生成/导演、故事质检、选定故事生成剧本、剧本编辑/确认/生成分镜、分镜完整编辑与版本。新增报告、确认决定持久记录；质检失败/问题/待运行/过期均可保留继续，只拒绝空内容/来源冲突/非法引用。自动质检、修复建议先预览采用后版本追加及复检；剧本修复含原故事+全文报告；分镜 schemaVersion2 保留全部 shot/line ID 集合、摘要一致及引用校验。生成迟到不覆盖新版本，上游更新递归过期；unknown 显式确认重试，幂等复用扩展所有文本任务。冻结独立 interactionKey 与 Task1 配置。先测试核心冲突和事务，再实现，完整回归。

### Task 3: 工作台与配置界面
生产 React 沿用现有设计，实现 Task1 API 的设置三 Tab、模型紧凑文字树及可保留草稿、共用资源库、具名方法选择。创建项目画幅/分辨率及统一规格设置。创意助手无方法；故事确定生成剧本、剧本确认生成分镜；剧本分镜编辑、质检建议/保留继续、版本历史、失败恢复。仅必要抽取共享请求/导演/版本组件。真实调用后端，浏览器测试覆盖完整流程与冲突恢复，不伪装媒体生成。

### Task 4: 集成验收与审查
运行迁移升降、后端真实 DB/Redis Worker、契约、静态、浏览器、相关 PRD 校验。增量审查以及全分支审查。真实 DeepSeek 注入后执行到确认分镜；若凭据仍缺失，记录未验收事实及可执行入口，不能宣称 M1 已全部通过。更新工程事实文档，保留用户 PRD 改动。
