# M2 最终真实验收 · 2026-09-09

状态：M2 工程与真实媒体验收完成。剩余中镜故障恢复专项、真实页面、最新备份恢复及独立最终复核均通过。按 R5 的 M2 退出条件验收，不将此结果等同于 M3 成片或最终 UI 验收。实施与预算方案见 [本轮计划](m2-final-acceptance-plan.md)，此前真实验收见 [累计记录](m2-real-verification.md)。

## 已完成范围

真实 Seedream 参考图、人工确认、两角色逐台词 MiniMax 音频、至少三镜 Seedance 视频及实际前镜尾帧引用已在此前通过。保存/重载、受理后重启恢复、上游修改失效、跨项目隔离、备份迁移及独立代码复审均有既有证据。本轮补齐第二镜失败阻断第三镜、恢复不重做第一镜。

使用原本地项目 `8916ef02-2715-468f-86fa-c5dfc84673a8`「M2 真实媒体验收 · 卡通小动物送果子 · 三镜」，分镜 revision 5。复用既有7张卡通图片和2句配音，新建3镜各4秒/720P/16:9顺序链；没有新增图像、音频、TOS或素材库调用。

## 故障和恢复证据

| 镜头/操作 | 本轮任务 ID | 外部任务 ID |
|---|---|---|
| 第一镜成功 | 691a65ca-3dcf-4f8c-a0db-5e4a29533cfa | cgt-20260909123747-44hcz |
| 第二镜原任务，尾帧失败 | 8dd661c6-5351-4747-94f3-c3b564e43ec1 | cgt-20260909124028-m79vv |
| 第二镜页面重试成功 | 235a6f5e-5f73-4242-bee9-81d078479044 | 同原第二镜 |
| 第三镜恢复后成功 | 580e4b8a-7959-4ef1-8ba5-408818dfe54d | cgt-20260909124350-rvs5t |

只在本地Worker进程的PATH加入一次性FFmpeg包装器，精确匹配第二镜暂存视频路径、尾帧参数和目标PNG。无网络自检确认其他调用透传、指定调用仅失败一次。第二镜视频确由真实供应商生成，已下载并持久保存收据后，本地尾帧提取返回 `last_frame_extraction_failed`；这不是供应商拒绝，不是用替身生成结果。

失败时第一镜成功，第三镜 `waiting_dependency`、`submitted=false`、`external_id=null`。通过真实页面展开失败任务，错误状态可见、重试按钮可达，点击“重试此任务”成功。重试沿用原第二镜的 external_id、staged_key、视频SHA256；走本地文件完成路径，不重新创建视频或查询供应商。原失败历史保留。

独立断言确认第一镜整个证据记录（任务/结果/文件/尾帧/事件）在故障前后完全一致；第三镜父任务改挂第二镜重试，前镜结果ID和尾帧ID精准匹配恢复结果。全轮3个不同供应商视频ID；提交次数判断结合现有执行路径和已保存收据，不把重试job数量当成付费创建次数。

## 文件、播放、费用和恢复

新增视频文件为 `dc7cf9c5-c544-542b-8eaf-affe3a2d5678`、`4731e737-a189-58df-8a8e-95d592a2c91f`、`5e2486a3-1529-5084-9a8f-ff92f9dee1f3`。各4.041667秒，共12.125001秒，1280×720/H264；六个视频及尾帧通过HTTP读取/SHA256核对、跨项目404。页面实播包括本轮3视频、旧3视频和既有2配音，刷新重开项目保留；旧播放脚本的固定输出“3 videos”未计入历史视频，逐文件记录实际为6视频/2音频，已改为动态统计后复查。

实际usage共261900tokens，按此前用户确认的Mini四折与23元/百万tokens估算 `2.40948元`，低于本轮5元上限；供应商账单未核对，不能写成实际扣款。故障恢复仅1次且复用本地视频；没有额外付费重试。

故障配置已清空，含包装器PATH的本地服务已停止，重新以正常PATH启动，恢复后再次核查播放与重载。最终停写备份 `.local/backups/20260909T044822312206Z`，`make restore-check` 在隔离数据库和迁移媒体目录通过10项目/35文件核对。仅保留既有Starlette弃用警告。

证据在本机忽略目录 `.local/m2-final/`：state.json、first-succeeded.json、failure.json、resumed.json、final.json、verification.json、fired.json、failure-ui.png、browser-verification.log、browser-after-restart.log、completed-ui.png。证据不含密钥；生成媒体和凭据不入库。

## 验证边界和交付位置

本轮没有修改产品代码。分支收尾另行实际执行完整回归：make test 147项通过（24.48秒，4个既有弃用警告）、OpenAPI一致、TypeScript/Vite构建通过；make test-ui 29项通过（36.6秒），make check及git diff --check通过。以上隔离工程/替身回归与本轮真实媒体链、页面、文件及备份恢复证据分别记录，互不替代。既有四足姿态/画面道具等美术差异、分镜长表密度、错误码窄列截断等视觉问题继续记录在 UI 差距文档；未发现阻止本轮重试的遮挡或不可达按钮。

真实验收限于已选模型及非真人卡通路线。未验证写实人像素材库路线、供应商所有故障类型、完整逐帧美术与叙事质量、实际账单或容器部署。音轨/字幕/基础剪辑/合成MP4及用户最终视觉验收仍属M3及后续，不属于本次M2关闭声明。

本次新增媒体仅在本地，公网仍保留此前部署和上一轮验证结果；本地与服务器没有自动同步。本轮不推送、不合并main、不部署、不进入M3。开发分支为 codex/m2-media-chain，已快进纳入main的既有部署记录。

独立最终复核通过：另外重算全部35个备份文件及本轮6文件在当前/备份两处SHA256一致，独立ffprobe确认三段720P视频时长，复核恢复依赖、失败历史及第一镜不变，无阻断。备份保留2个既有配音unknown历史任务，不是本轮活动任务，未自动重投；其历史计费不确定性仍按累计记录保留。

## 归档授权补充

用户在M2验收完成后明确要求将本轮记录提交、合并main并推送GitHub，覆盖上述检查点“不推送、不合并”的限制。本次仅归档工程和验收文档，不迁移新媒体、不更新服务器、不启动M3。
