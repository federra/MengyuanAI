# 侧栏与分镜紧凑交互验收（2026-09-10）

基线aa9cf0c，分支codex/v12-v13-ui-alignment，目录 /Users/nathan/Codex_Projects/MengyuanAI/.local/ui-v12-v13。范围以用户最新反馈为准，参考图只用于元素比例。计划见compact-workspace-plan.md；本文件记录已完成事实。

## 原型先行与实现

先修改PRD07/08、R1/R2/R5及共享导航/分镜/元素原型、分镜标注；原型代码和尺寸确定后才开始生产增量，原型验证与生产实施随后并行。未修改runtime阅读器或原始工作区中的未提交PRD/ZIP。

- 侧栏188px展开、68px收起（窄屏展开150px），五个入口保留可访问名称/title，偏好持久化；项目文件夹、创作锤子、资产宝箱、任务列表、设置齿轮采用同色描边SVG。收起不卸载工作台，正文草稿和主题保持。
- 分镜标题下单行工具栏：角色管理、场景管理、道具管理、批量配音、批量站位图、生成设置、导入分镜JSON。按钮44px，空间不足时工具栏局部横滚；七项均打开实际管理/配置窗口。原有顺序/独立视频生成按钮保留在表格上方。
- 六列表头按要求改为序号、台词、角色/场景/道具、提示词、视频、操作。列宽基准56/240/232/360/220/72px，合计1180px；台词64px、提示词默认160px、参考缩略图48px，四类引用2×2紧凑排列，站位功能保留。图片选择、描述、稳定ID及绑定详情按需展开；仍可查看完整内容，不截断保存数据。
- 全片元素管理改为190px列表与选中编辑区，弹窗最大1040px，描述88px、图110px；明确“管理图片”入口复用真实实体图片生成/上传/确认。切换对象、关窗和冲突保留草稿。初次读取参考图/任务时显示加载且禁生成，避免误报空列表。
- 仅已确认有效图片可新绑定，保留旧引用的待更新状态；替换继续使用原引用ID。参考图状态在BoardMedia集中读取，避免每镜重复轮询。
- 批量配音/站位先展示当前候选、继承参数及跳过原因，用户确认才提交；配音可逐句选表现，语速1倍。现有结果、在途/未知/失败原任务不自动另建付费任务。逐项受理并记录，局部失败保留未提交项；网络未知冻结原正文/配置/请求身份，恢复后复用同一幂等键。接受任务后的刷新失败不会误报未提交，关闭重开也不会重发已受理目标。
- 生成设置复用现有项目规格API与模型/风格配置；关闭保留当前项目配置草稿，跨项目迟到回执只更新仍在查看的同一项目。JSON继续使用既有整表事务导入、预检/冲突/历史/草稿机制。

未改schema、API契约、后端模型适配或历史快照，未新增付费调用，费用0元；不合并、推送或部署公网。本地Vite已加载源码，不需要停写、迁移或重启API/Worker。本轮未另做备份恢复，既有停写备份20260910T054447364699Z及12项目36媒体恢复记录保持，不能写成新增演练。

## 工程与视觉证据

- 原型validate、V5/V13纯JS、V13完整Chrome以及新增紧凑交互检查通过；四主题1600×1050与1280×900无页面横溢出、导航键盘/持久、实体切换草稿、批量弹窗。详见interaction-prd-workspace/reference/verification-compact-workspace.md，证据evidence/compact-prd-*.png。
- 生产页面全量77项通过；之后修复两项独立复审P1及工具按钮焦点，再跑12项受影响测试通过，新增1项未知网络原身份重试通过；最终图片加载修复另跑6项通过。当前80项累计通过，非最终单次全量80通过。测试在5181使用API替身，不代表新供应商验收。
- 真实PostgreSQL隔离专项16 passed、200 deselected，涵盖实体版本/跨项目、图片来源/确认、稳定绑定、单镜配置等既有机制；未直接连接生产执行测试。契约、TypeScript/Vite构建、ruff、diff检查通过。
- 公共导航九类页面四主题覆盖，188/68px尺寸和草稿保持均验证，新增36张折叠截图在evidence/production/（替身数据）。本轮未将所有视觉差异写成用户接受。
- scripts/compact-live-readonly.mjs使用真实5180与既有卡通项目，所有API写请求一律拦截；四主题七弹窗与工具顺序、实际元素参考图读取、焦点进入/关闭返回通过，API写入0、浏览器异常0。Chrome原生200%（innerWidth720、DPR2、visualViewport.scale1）生成设置/JSON导入主操作可达，页面无横溢出。截图/报告evidence/compact-live/，真实数据与替身分开。其余弹窗200%未逐一覆盖，不扩称全面适配通过。
- 独立复审两个P1已关闭：A项目规格回执切回A；已受理批次刷新失败造成重发。真实截图发现工具栏display:contents误用于直接按钮导致失去盒模型/焦点，已限制为包装容器并增加七按钮≥44px断言；初次图片假空状态已修正。
- 既有真实MP4仍可HTTP200下载，SHA256为74f339448e97dfedf532c42bd51831230b50d72cc011c3e1ad6b86840e6a8267，与此前一致；未新增真实成片或全链验收。

U01～U03按本轮结构、字号与四主题尺寸检查；U04～U06由隔离交互/PG及异步故障回归验证；U07键盘焦点及上述原生缩放局部通过；U08待用户。工程、真实媒体/成片、M3用户独立全链和最终视觉接受分别记录。

## 精简试用与剩余项

刷新http://127.0.0.1:5180/ → 点击左侧“‹”收起侧栏 → 项目/创作/分镜。先收起导演助手查看表格最大宽度，再检查七个入口、表头和行内比例；打开角色/场景/道具，选择已有元素→管理图片，核对原图、编辑与保存入口。切换四主题复核。批量按钮先打开配置清单，点击提交才会产生实际供应商请求；本轮可只检查开关窗口、选择与草稿，未自动授权额外试生成。

新增尺寸与布局须由用户确认是否合适；最终全范围视觉验收、M3一句话独立成片仍待用户。原型仍有演示能力（如示例图和资源库演示），不能当成生产新接入证据；本轮未扩展全新跨项目资产分发或多轨编辑器。原型服务43971仅本地审阅，生产入口仍5180。
