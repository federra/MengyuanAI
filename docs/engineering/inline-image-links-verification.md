# 角色括号链接与图片预览 · 2026-09-11

本轮只调整提示词图片引用，生成按钮规则未修改。先补PRD07、R2/R4/R5及R4机器清单，更新原型；沿用现有稳定ID、图片存储、ImagePreview和分镜版本保存。

## 实现

- 提示词默认原位展示可点击链接，点击打开该ID实际绑定的完整图片，可关闭或Esc退出；待制作/无图明确提示，不按同名图回退。点击普通正文或“编辑提示词”原位编辑，继续支持@选图；“返回图片链接预览”仅切换视图，不冒充保存。
- 旧分镜角色尾部引用在当前显示中定位到正文角色名之后：角色名（@角色名），保留场景、道具原位置。按本镜明确角色引用ID和唯一名称处理；重名或正文无匹配保留原位，不猜测归属。重复排版不重复加括号。历史版本不批量修改，编辑后通过既有保存另存版本。
- 新AI分镜应用约束及目录发布逻辑使用角色括号引用；后端发布保留稳定ID。生产Worker已加载新代码。本轮不调用模型，不宣称真实新AI输出已验收。

## 按钮诊断

8848当前分镜revision4、version2b01d01e-819f-437e-9a3f-c85db3c171ed已保存、stale=false，confirmation=null。两个按钮共用BoardMedia.enabled，条件包括无未保存变更、基准版本一致、未过期、已确认且非忙碌；批量按钮/单镜按钮另检查执行中任务。保存不等于确认，当前缺确认是共同禁用原因。确认入口为右侧AI导演助手“保留当前版继续”，不要求AI质检通过。未替用户确认或改动门槛。

## 证据与边界

- 隔离make test：242项后端、OpenAPI一致、前端构建通过。新增角色尾部迁移、括号幂等、同名不误绑测试。
- 5项相关页面测试通过，覆盖稳定ID、改名、旧尾部排版、选图、点击预览与Esc关闭、草稿重载及既有图片计数。
- 实际8848四主题图片链接与解码图片检查，所有非GET API被拦截。evidence/inline-image-links/live.json、四主题prompt/preview截图；已人工查看noir提示词截图。
- PRD validate与V13原型回归、make check、构建通过。
- 确认无在途任务后正常停止本项目旧API/Worker/UI，备份.local/backups/20260910T223011869382Z；隔离恢复15项目43文件通过，再启动本地5180/8010。无数据库迁移、无当前分镜批量改写，无新增付费、合并、推送或外网部署。

最终视觉与独立全链试用仍由用户验收，自动回归不代替用户接受。
