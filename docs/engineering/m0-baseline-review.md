# M0 Git 基线审查 · 2026-09-08

基线提交 `cf2d2c6f1823062b9be1f834598c602953fa760d`，标签 `m0-baseline-20260908`。保存前工作区干净；`make test` 9 项通过、OpenAPI 一致、TypeScript/Vite 构建通过；`make check` 通过。仍有验收记录已说明的两条上游弃用警告。

只读代码审查覆盖任务行锁、Job/Outbox 同事务、幂等、租约旧执行者拦截、内容修改冲突、项目/owner 隔离、图片校验和路径穿越、备份恢复。未发现阻断基线保存的 P0/P1。未读取凭据文件，Git 跟踪路径中没有 `.env`、私钥或 `.local` 数据。

发现 P2：`scripts/local.py` 用 Redis PING 判断是否复用 56379，未验证实例属于本项目。其他 Redis 占用同端口时可能被复用；在 M1 开发中补充实例目录归属校验及回归。基线保留原始实现，后续修复独立追踪。

范围不变：此标签保存 M0 本地原生进程工程；没有新增 Docker、真实 AI 或成片通过结论。M1 故事闭环在 `feat/m1-story-loop` 分支继续。
