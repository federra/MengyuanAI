# V12/V13 工程与视觉交互检查点

2026-09-10。本轮在既有M0–M3工程上增量实施，不重建、不新增供应商调用。**生产升级待用户授权；用户视觉与交互验收尚未通过。**

## 基线与实现

隔离分支`codex/v12-v13-ui-alignment`，源代码`cad6c2d`，叠加原目录最新未提交PRD；文档基准SHA256见`.local/ui-v12-v13/baseline.json`。原运行目录代码、PRD与ZIP保持。差异及最小顺序见`v12-v13-implementation-plan.md`。

已实现V12：创建上移、三列项目卡、五阶段吸顶、115px创意框、方法/数量/生成横排、移除创意助手；故事数量1～3严格校验、按项目保存、任务冻结、幂等重放、实际输出数量检查。仅改数量不使已验收内容失效，初始读取不生成伪草稿；历史模板继续读取，新生成模板要求storyCount变量。

已实现V13：六列分镜、逐句TTS及配置/历史、单镜模型/画幅/分辨率覆盖、风格正文与图片顺序/台词ID进入供应商请求快照、独立批次任务及持久并发上限。依赖前镜尾帧或已有当前结果/活动任务的镜头给出跳过原因，受理过的失败走原任务重试，避免重复收费。

JSON文件/文本预检、事务整表提交、新ID映射、描述元素、单镜覆盖、旧历史保留、确认清除与成片失效、幂等查询均已接入。旧预检在换文件时作废；服务端成功结果不依赖浏览器缓存才能显示；迟到轮询不能恢复旧表或旧确认。描述素材可绑定已确认真实图片，未制作素材不能冒充已就绪。

## 工程证据

- 完整隔离PostgreSQL/Redis/Worker回归：**207 passed**，包括24项导入、14项数量/模板及10项V13媒体测试；旧M2/M3测试保留。4项既有依赖弃用警告，无供应商调用。
- 完整浏览器隔离回归：40 passed；随后新增导入无效UTF-8作废旧预检、丢响应查询原提交成功验证通过。最终40项全量页面回归通过；公共弹窗焦点循环与关闭恢复均通过。
- OpenAPI/TypeScript契约匹配，Vite构建通过；`make check`通过，PRD validate：23模块/11页面/68关系。
- 旧M3备份隔离恢复及V13升级：34旧表逐表内容不变、36媒体哈希一致，新API读项目/分镜/剪辑通过，演练库删除。详见`v12-v13-local-upgrade.md`。
- 独立后端审查发现的映射后超长提示词和异常大数已修复：预检字段422，不在提交时500。媒体自查修复旧快照图片顺序兼容与同供应商ID恢复后的并发额度释放。
- 前端审查修复更换文件、提交中继续编辑、缓存失败与迟到确认的四类竞态；最终独立前端静态复审未发现确定P1/P2。

## U项证据与边界

固定Chrome、1440×900主视口，1280×800/1920×1080补充；四主题共用R1配色/字号。原型64张截图与computed记录：`evidence/prototype/`；生产前端截图与computed记录：`evidence/production/`。均为独立浏览器数据，不冒充真实生成结果。

| 项目 | 当前证据/结论 |
|---|---|
| U01 | 九页面四主题覆盖，三列、115px、六列、逐句入口与单镜/导入入口断言通过；原型3镜与生产11镜压力样本分别标识，不能把数据不同作为布局偏差理由 |
| U02 | h1 28、菜单/流程16、正文15/分镜14、操作14、TTS/规格12；四主题muted正文对比均≥4.88；修复非例外小字和禁用按钮整体半透明。卡片标题16为小节语义 |
| U03 | 方法/数量/生成按钮边缘差≤2px；整页无横向溢出，分镜局部滚动；吸顶68px；修复悬浮助手被挡与创建表单空列。主要控件44px，模型树30px例外 |
| U04 | 真实API/PG任务与导入状态由隔离后端测试验证，供应商传输替身校验实际编译请求；没有新增真实供应商调用，不新增真实质量验收结论 |
| U05–U06 | 既有加载/提交/失败/过期/冲突回归保持；导入回滚、失联查询、草稿保留、版本冲突/跨项目和迟到响应已测；未返回媒体文件不标成功 |
| U07 | 新弹窗焦点循环、关闭恢复、减少动态效果通过。真实Chrome设置页选择200%，窗口1440×1000不变，innerWidth1440→720、DPR1→2、visualViewport.scale=1；浅色项目/创意8控件与设置弹窗可达，5截图及native-zoom200-verification.json留存。200%仅此范围，其他页面/主题未冒充已测 |
| U08 | 待用户明确验收。所有自动测试/截图均不代表用户接受 |

必要产品差异：生产保留真实保存/版本、任务幂等/恢复和素材确认控件；PRD演示数据与真实媒体状态不混淆。项目封面选择、TXT完整入口等既有未实现范围仍按原计划记录，不作为本轮已完成能力。4K单镜规格可保存，当前视频供应商不支持时明确阻止提交。

## 费用、交付及待办

新增供应商调用0，费用0元。复用旧M3的12秒720P成片证据保持，不将本轮替身回归写成重新通过真实成片或完整用户试用。

待办：记录本地提交号；获得具体停写/备份/迁移/本地切换授权后再更新5180；随后由用户验收四主题与主操作。生产入口仍为`http://127.0.0.1:5180/`，授权升级前显示旧版本。

### 升级后的用户试用步骤

1. 打开5180，在四主题间切换：项目创建应在上、记录三列，滚动创作页检查流程吸顶。
2. 进入已有项目的创意，修改文字与故事数量（1/2/3），切主题/设置后检查草稿；保存后重载核对。
3. 分镜检查六列、台词下配音入口、局部横滚、单镜规格设置；保存一个覆盖后重载，其他镜头规格不变，旧受影响媒体标待更新。
4. 用模板打开JSON导入预检；非法格式须保留原表。正式提交会产生自动建议质检模型调用，本轮不代点提交或生成。无费用的整表事务/恢复已在隔离工程中验证。
5. 检查模型/提示词/风格、元素/导入/单镜弹窗关闭与保存可达，Tab焦点和200%缩放；反馈页面、主题及具体问题。

工程测试不替代用户验收；用户未明确接受前U08保持待验收，M3独立一句话到MP4试用仍单独记录。
