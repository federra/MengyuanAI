# 参考图标尺实施与验收 · 2026-09-10

本轮在 codex/v12-v13-ui-alignment 既有工作区增量修改。未丢弃之前实体管理实现、版本或记录；未合并/推送/公网部署。费用0元，没有新增供应商调用。

## 完成事实

先更新正式PRD07/08、R1/R2、R4 Markdown/JSON、R5、U规则与原型；原型旧190px选中编辑样式已替换为真正七列表格（不是只改HTML或标题）。当前工作区原型入口 http://127.0.0.1:4174/，原4173未替换。

主内容不再受1700px上限限制。分镜台词卡仅在顶栏保留说话人、情绪、麦克风和删除，正文标签隐藏但无障碍名称保留；配音参数、生成、试听和任务记录收进弹窗。四类图片64px固定方框竖排，空框＋、有图缩略图，点击直接打开多选/绑定窗口，站位操作归入对应窗口。提示词下方显示模型/画幅/分辨率/各镜实际时长。导入按钮右侧增加界面设置，项目显示偏好刷新保留，不改业务数据。

三类管理窗口七列同时展示全部实体，统一事务保存原机制保留；参考输入上传/清除与输出结果分离。音色资源新增voice类别并复用已有PG版本表；角色可选择声音资产，无假音色。参考图/结果、生成配置确认、历史选择/确认、绑定和删除保护继续可用。生成中在图片格显示状态；配音错误直接显示弹窗，实体冲突在行中有比较入口。

分镜生成新任务冻结entity_catalog_contract=1，模型同一次响应输出目录与镜头/台词实体键；源是完整剧本正文，不额外发解析请求。服务端严格验证键/类型/镜头覆盖/台词数量，finish_job同事务写入实体、分镜引用和台词绑定。旧快照继续旧schema；不回填旧项目，不静默覆盖人工实体。解析失败旧分镜与旧实体保留；过期返回仅归档。此为工程实现，未进行新的真实供应商解析质量验收。

## 验证证据

- 完整 `PYTHONPATH=... make test`：233项通过，OpenAPI契约一致，前端构建通过。包含目录/绑定生成、非法目录原子失败、旧快照兼容及声音资产；M0–M3既有后端回归保持。
- 新四主题参考图标尺测试4项通过；严格元素工作流14项通过，输入/输出、统一保存、409草稿、未知结果原命令恢复、引用删除保护、跨项目晚回执隔离及资产复制均覆盖。
- 浏览器全量回归正在收尾，最终结果追加下文，不能把尚未完成的全量运行写成通过。
- PRD validate：23模块/11页/68关系；原型V5/V13契约回归通过。
- 真实5180只读：四主题×1440/1920/2560，共12组main计算样式max-width=none；四主题三类窗口截图，无pageerror、无写入。`evidence/reference-ruler/live.json`及同目录截图。
- 原型4174：四主题、12个实体窗口、麦克风弹窗，验证computed display=table与thead可见；`evidence/reference-ruler/prototype.json`及截图。不是生产供应商证明。
- 既有13项目36文件备份在临时库恢复，新代码读取通过；37个旧表原列/行摘要及36文件哈希不变。`evidence/reference-ruler/restore.json`。当前生产14项目36文件，新增验收项目尚需下次停写完整备份；不混淆两者。

## 尚未通过与本地检查点

5180前端已由Vite热更新；8010/Worker仍是此前启动的进程，新增目录/voice类型需要重新载入后端后才能用于生产写入。当前活动任务0，历史unknown配音2条保留，不自动重试。

当前不改各镜实际时长；统一默认单镜时长还是只移动现有时长，已请求用户选择，不能擅自改变现有视频/剪辑。最终视觉接受、真实新目录解析质量、用户独立M3全链仍未通过。物理200%浏览器缩放本次待复核，不以小视口或截图代替。

后端重载方案：检查仍无活动任务 → 短暂停写 → 备份当前14项目/36文件与当前工作区源代码 → 隔离恢复核对 → 使用相同媒体/凭据目录重新启动本地API/Worker/UI → 只读核对旧项目/媒体/MP4。无数据库迁移，无模型调用；失败保留备份，先恢复服务可读性，不自动重试unknown。

### 浏览器收尾结果

98项全量运行中96项通过；两项失败是旧工具栏7按钮断言及“本镜画幅”定位同时匹配新规格按钮和弹窗输入。按新原型改为8按钮、精确定位combobox后，单独复测2项均通过。属于96+2累计验证，不称为一次完整98项通过。U测量覆盖四主题九页、焦点/滚动/小视口与减少动效；物理200%仍不计入此次通过。最终静态检查、git diff --check、manifest校验均通过。

## 用户批准后的本地重载（2026-09-10）

用户批准上述具体停写方案后，确认活动任务0、历史unknown配音2条，停止旧API/Worker/UI，完成最新备份和隔离恢复后重启。本节替代上文“API/Worker仍待重载”和“14项目尚需备份”的检查点状态。没有生产数据库迁移，schema仍为20260910_entity_workflow；没有新增供应商调用或重试历史unknown。

- 最新备份：`.local/backups/20260910T142519132033Z`，包含14项目、36媒体文件及589个源码文件的独立归档和哈希清单；保留未提交实现，不包含凭据。源码归档冻结于重载前，本节为后续验收记录。
- `make restore-check`通过；额外隔离恢复逐一读取项目、分镜、剪辑、实体、参考图片及导入接口，40表原行摘要、36媒体哈希不变。证据：`evidence/reference-ruler/restart-restore.json`。
- 使用原媒体和凭据目录重启集成工作区本地API/Worker/UI。重启前后40表摘要及36文件哈希相同：`evidence/reference-ruler/restart-audit.json`。
- HTTP读取14项目全部相关接口成功；OpenAPI已含voice资源类型，声音资产当前0条，不填造假预设。原M3成片下载返回video/mp4、5,444,867字节，SHA256仍为`74f339448e97dfedf532c42bd51831230b50d72cc011c3e1ad6b86840e6a8267`。证据：`evidence/reference-ruler/restart-http.json`。
- 重启后真实5180只读浏览器复核四主题×三视口12组以及三类管理窗口，错误0、拦截写入0：`evidence/reference-ruler/live.json`与截图。没有更改既有项目内容。

本地入口http://127.0.0.1:5180/已可试用。新目录契约仅适用于后续新建的分镜生成任务，不回填旧项目；真实模型解析质量未在此次免费工程验证中验收。各镜时长保持原值，统一默认时长决策、物理200%复核、用户最终视觉接受和M3独立全链试用仍待完成。未合并、推送或公网部署。
