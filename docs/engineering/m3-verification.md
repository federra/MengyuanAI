# M3 实施与验收记录 · 2026-09-09

状态：**工程回归及复用真实素材的MP4合成验证通过；用户独立一句话→MP4尚未试用，M3不整体关闭。** 本地可试用，未合并、推送、部署或进入M4。范围与基线见[m3-implementation-plan.md](m3-implementation-plan.md)，用户操作与待批准用量见[m3-user-acceptance.md](m3-user-acceptance.md)。

## 基线与实现

实际开始时main为9c077d4，工作区干净，M2实施43af009与最终记录9c077d4已提交。归档授权记录解释了历史分支状态；没有未提交M2文件，不制造空提交。重新跑147项基线回归后建立本地注释标签m2-baseline-20260909→9c077d4，创建codex/m3-real-export。没有丢弃既有记录。

新增finishing领域、export_versions不可变剪辑与export_commands命令别名。复用现有项目锁、配置/来源快照、稳定ID、Job/Outbox/Worker租约、媒体存储与M2来源校验。提供排序/裁剪/逐句配音/原声/上传音乐/字幕、真实预览下载。台词按真实时长定位，重叠和超长阻断；背景音乐显式循环至片尾，输出限幅。规格继承项目，默认留边，可选裁切，不静默拉伸。字幕Pillow渲染中文后FFmpeg叠加，依赖中文字体，缺失明确失败。

相同当前来源去重，失败保留版本可重试；编码中断只重算本地，不调用供应商。执行前后核对哈希，提交阶段锁定项目与租约；只有文件成功且输入仍当前才标项目完成。上游变化使完成状态失效，旧成片可下载，迟到结果仅保留历史。独立复审发现并修复同内容新配音误复用旧成片、完成状态失效遗漏、跨键去重与跨端点幂等漏洞、迟到上传/轮询/初始化覆盖草稿；最终复审未发现新的必修问题。

## 工程证据

- `make check`通过；`make test` **159项通过，47.16秒，4条既有弃用警告**；OpenAPI一致，TypeScript/Vite构建通过。统一使用shortfilm_test_*隔离PG、系统临时媒体及真实Redis，未直接pytest连接生产。
- `make test-ui` **32项通过，42.1秒**，独立5181/API替身；含M1/M2回归、M3保存/重载、迟到轮询与上传不覆盖草稿。替身页面结果不代替真实供应商或成片。
- 新增测试用真实FFmpeg编码验证字幕特殊字符、尺寸/fps/时长/AAC、原声800Hz+循环音乐400Hz同存、居中裁切、有效音频上传与伪音频拒绝、跨项目读取/引用拒绝。三镜完整合成、台词超长、过期源、同内容新TTS、失败/重试、迟到历史结果、源哈希破坏和幂等冲突均覆盖。
- 真实Redis Worker在clip-0编码时SIGKILL，过期租约恢复同job后重启，重复4条队列消息只产生一个结果、两次attempt；使用本地编码夹具与供应商替身，未新增收费。测试见tests/test_finishing.py、tests/test_finishing_processes.py。
- 独立只读复审两轮问题均修复并回归，最终无必修项。PRD文档校验与最终工作区检查见收尾补充。

## 真实成片与页面

复用项目8916ef02-2715-468f-86fa-c5dfc84673a8“ M2 真实媒体验收 · 卡通小动物送果子 · 三镜”，分镜v5、既有7张确认参考、2句真实MiniMax配音、最新3段真实Seedance视频。没有生成/替换原供应商素材。原视频各4.041667秒；剪辑明确选各4秒，成片12秒，比素材总12.125秒少的尾部画面是已选裁剪，不涉及台词截断。

通过真实页面保存剪辑v1并提交，任务73971d79-70ed-4606-b3c4-20bc1d0e2917成功，输出b1e3d55c-96bd-40db-9d59-c9946d120e5a。MP4为12.000秒、1280×720、24fps、H.264/yuv420p、AAC/48kHz/双声道，5,444,867字节；配音音量1，字幕开启，原声关闭，无背景音乐。浏览器实际播放至ended=true，无解码错误；下载入口点击可达，HTTP attachment下载与预览字节完全相同，跨项目下载404。

完整FFmpeg解码通过，全片blackdetect无黑帧；0.5、1.8、3.95、4.05、8.05、11.8秒抽帧人工查看，三镜及“送给你。”“一起吃！”字幕正常。两句真实原音频与成片解码比较相关系数0.999629/0.999771，起点0/1.3秒、长度1.3/1.149406秒、最佳时差0秒，无漏句或静默截断。该证据不是用户主观听感、美术/叙事质量接受。

页面重复“生成真实MP4”仍只有原任务/结果；刷新重进保留文件名、剪辑v1、12秒预览与下载；项目显示已完成/导出阶段。迟到请求草稿问题已修复，当前实页无阻断操作的遮挡或不可达。镜头稳定ID占位、长列表密度及最终原型视觉对齐仍待统一处理，不宣布UI验收通过。

成片副本：`.local/m3-real/卡通小动物送果子-M3真实成片.mp4`；正式存储：`.local/media/projects/8916ef02-2715-468f-86fa-c5dfc84673a8/b1e3d55c-96bd-40db-9d59-c9946d120e5a`。SHA256：`74f339448e97dfedf532c42bd51831230b50d72cc011c3e1ad6b86840e6a8267`。证据在忽略目录.local/m3-real/：preflight.json、migration.json、first-export.json、export-job.json、ffprobe.json、verification.json、reload-dedup.json、contact-sheet.jpg和六张frame PNG；不含密钥，不加入Git。

## 迁移、备份与剩余边界

升级前停写备份`.local/backups/20260909T053336917287Z`，隔离恢复10项目35文件通过；M3增量迁移前后10项目、35文件、39内容版本、71任务、27媒体结果以及全部既有文件哈希不变。只新增两个导出表。

成片成功后再次停写备份`.local/backups/20260909T055254635250Z`，`make restore-check`通过10项目36文件。另一次性隔离数据库和系统临时媒体恢复核对剪辑v1、原冻结job快照、项目完成状态、预检无阻断及下载SHA256完全一致，见restore-export.json；恢复库和临时媒体已清理。历史unknown保留不自动重投，凭据不纳入普通备份。服务已以正常PATH恢复，重启后核验另记收尾。

本轮新增供应商调用0，新增供应商费用**0元**；不把已有素材历史费用计作本轮扣款，也未核对历史账单。音乐/原声混合使用本地声音夹具验证，不声称真实卡通成片包含背景音乐。容器字体配置已补但未运行容器。独立用户新项目全链待执行，最小方案及≤5元用量需用户确认后才开始；现有手工准备媒体不能代替此项。最终UI接受、M4、推送/部署均未开展。

收尾复核：服务重启后再次通过正式HTTP下载、字节一致性、完整解码、黑帧扫描与逐句音频对齐，参数/哈希不变。PRD校验通过23模块/11页面/68关系，git diff --check通过。当前本地开发服务保留供试用；新增数据库和媒体记录已包含在上述最终备份。实施和验收文件随当前M3分支实现一并本地提交，回退M2标签不变。


2026-09-09用户随后确认继续，5元三镜两句方案已获批准，覆盖此前“待批准”状态。当前仅准备独立试用环境、设置临时4096文本输出上限及空调用台账；新项目仍待用户创建，没有新增供应商调用或用户验收结果，M3整体状态不变。
