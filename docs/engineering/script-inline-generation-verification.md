# 剧本正文与文本生成状态增量（2026-09-10）

基线 c1ffe24，分支 codex/v12-v13-ui-alignment，实施目录 /Users/nathan/Codex_Projects/MengyuanAI/.local/ui-v12-v13。按用户最新两条反馈增量修改；未重新实施M0～M3。

## 差异及完成事实

- 旧剧本页仍有估算秒数、场景结构、保存/载入基准及本次分镜生成要求；现均从常态页面移除，完整格式正文保留在一个文本框，停止输入700ms后自动保存新版。版本冲突/保存失败时才出现恢复或重试入口，保留用户草稿。
- 点击生成分镜先完成当前正文保存，再使用返回的真实版本入队；不再读取已删除的补充要求。API沿用既有ScriptBody及PostgreSQL版本契约，手工正文重建派生结构，不把旧场景/台词当成新输入。新任务编译约束明确source.text为唯一叙事依据，估算秒数不能用于删减正文；历史资源与快照不改写，无schema迁移。
- 故事、剧本、分镜主生成及导演修改/修复、质检输出，在对应输出文本框直接显示精确“AI生成中...”。初次无结果也有输出框；分镜为整表输出区域。建议/质检生成不替换尚未采用的源正文，新故事批次使用独立“新故事方案”输出框，不锁住旧候选草稿。
- 状态使用真实任务的项目、种类、目标/源版本与目标修订匹配；排队、运行、等待供应商/依赖显示，占位只在视图层。成功/失败及unknown状态不显示生成中，占位不保存到正文或草稿；接受任务后立即更新局部状态，轮询继续恢复。已有手动输入、媒体提示词和配置框未被AI生成时保持可编辑。
- PRD06、R2/R4（Markdown/JSON）/R5与共享原型同步。未引入新供应商请求，费用0元；未修改凭据、迁移数据库、合并、推送或公网部署。

## 验证证据与边界

- 后端全量215项通过；其后新增真实PostgreSQL“手工正文保存→重载→分镜冻结快照→同键重试→历史及跨项目隔离”专项，与提示词契约共2项通过、214项未重复执行，累计216项。契约、TypeScript/Vite构建、ruff、diff检查通过。模型仅测试配置替身且专项不执行Worker，不代表新供应商验收。
- 页面71项累计通过：全量初跑66项通过，5项旧断言/测试选择器适配后，最终受影响集16项通过（有重叠，并非宣称最终单轮71全绿）。新增剧本5项自动保存/冲突/丢响应/刷新专项与15项AI目标状态测试；覆盖正文优先、旧任务/终态、初次生成及建议不覆盖源文。独立复审发现“新故事批次误遮旧候选”P2已修正，并验证旧正文仍可编辑。
- API替身状态截图：evidence/script-loading/{light,dark,sky,noir}-script-generating.png，1440×900。真实5180只读截图：evidence/script-loading/live/；scripts/script-live-readonly.mjs通过四主题及Chrome原生200%（innerWidth720、DPR2、visualViewport.scale1）正文/生成分镜按钮可滚动到达检查，零API写入、零浏览器异常，生成按钮仅trial未点击。首轮脚本刷新后漏了项目导航，修正后复跑通过。
- 原型validate、V5/V13静态与reference/tests/script-loading-browser.mjs通过；原型证据在interaction-prd-workspace/evidence/script-loading/prototype/，8张截图。原型本地存储与生产PostgreSQL证据分开，截图不加入版本库。
- U01～U03受影响结构/文案/四主题共用样式核对，U04～U06用隔离状态/版本/失败测试验证，U07真实200%局部可达性通过。未声称所有页面/所有状态已经视觉验收；U08用户最终接受及M3独立全链仍待用户。

## 本地加载及数据保留

沿用此前授权，本次确认无活动任务后只停止本项目应用监督进程。最新停写备份 /Users/nathan/Codex_Projects/MengyuanAI/.local/backups/20260910T054447364699Z，12项目/36文件隔离恢复通过。API/Worker/Vite已从本工作树重启，无数据库迁移；PG、Redis、PRD服务不变。

重载前后38张表的行数及内容摘要全部一致，历史unknown任务未重发；既有M3成片下载HTTP200，SHA256仍为74f339448e97dfedf532c42bd51831230b50d72cc011c3e1ad6b86840e6a8267。证据evidence/script-loading/local-reload.json及before/after-reload.json。仅证明已有成片保留可用，未新增真实成片验收。

## 用户试用

1. 刷新 http://127.0.0.1:5180/，进入已有项目→创作→剧本；确认正文下只保留分镜方法与生成主按钮，没有本轮删除项。
2. 修改正文，停止输入观察自动保存；刷新并重新打开项目，检查完整换行、内容及历史版本。切换四主题复核。
3. 按已有费用授权自行执行实际生成时，检查目标文本框出现“AI生成中...”，完成后替换结果；生成分镜以点击前最新正文为准。导演建议不应覆盖未采用的正文，追加故事不应遮住旧候选。

工程通过、真实供应商/成片、用户独立全链和最终视觉接受分别记录；本轮完成前两项交互实施与工程验证，不替用户宣布验收通过。
