# 风格模板与具名图片引用 · 2026-09-11

本轮在 codex/v12-v13-ui-alignment 增量修改；用户授权所有非付费操作。新增供应商调用0。未合并、推送、外网部署。

## 需求及实现

先更新PRD05/07/08、R2/R4/R5及原型风格、提示词编辑。上一轮图片预览、自动引用和引用数量要求保留。

- 现代电影、古风写意、清新动画三份完整风格正文：材质、光影、色板、运动及连续性约束。使用已有Resource/ResourceVersion表与编辑API，确定性种子ID，只补缺失，不覆盖用户版本或项目选择；未迁移数据库。项目选择后沿用实际正文的分镜输入、视频编译、配置快照与既有失效判断。
- PromptEditor输入@打开本项目已有生成图片窗口，图片有缩略图、名称、类别和状态；可关闭。选中同时写入本镜refs及prompt中的稳定ID。显示投影为@名称，普通编辑保留未改动引用ID；改名、重名不以名称反查猜测。手工编辑标签取消该文字标记，不自动删除图片栏引用。待确认、过期素材仍由既有生成校验阻止使用。
- AI分镜生成提示词要求实际风格表达、实体名后具名标记；发布时用显式shotEntities/catalog键转换为稳定ID。同名无法唯一判断时保留独立显式引用，不把两图合并。旧任务快照和已保存分镜不自动重写。

## 时长与按钮检查（未修改）

检查项目8848：分镜revision3，version94507deb-e2f6-45f4-8b3f-7967d50ffccd，stale=false，confirmation=null。缺少当前版确认使BoardMedia.enabled=false，因此生成本镜视频禁用；本地未保存草稿、版本冲突、忙碌、同镜活动任务也可能禁用。AI质检不是门槛。

当前8镜时长15/20/12/18/15/15/25/15秒，直接来源于shot.duration，可从AI、JSON或人工修改而来；新增镜头默认3秒，不继承统一时长。代码中的Seedance2.0适配器校验4～15整数秒，2.5上限30秒。建议显示禁用原因和模型时长约束，长镜人工拆分/调整；不得静默缩短、删台词。以上仅诊断和建议，不改用户当前分镜或按钮规则。

## 验证与证据

- 隔离make test：241后端通过，契约一致，前端构建通过。含三风格种子重跑保留用户版本、目录重名/前缀防错；现有V13测试检查实际风格正文进入供应商请求、台词与引用快照。
- 相关Playwright六项通过：名称/稳定ID、重名改名、输入@选择、草稿重载及既有生成图片保存/预览/计数。早期失败来自测试未提供files列表和刷新未重新进入工作台，已修正测试场景并重跑。
- 实际8848四主题只读浏览器检查，所有非GET API拦截；各主题窗口可打开/关闭，编辑框不暴露UUID。证据evidence/style-mentions/live.json及四主题picker.png；已人工检查noir截图。
- PRD validate通过23模块/11页/68关系；v5、v13原型静态回归通过；make check通过。
- 停写备份.local/backups/20260910T214629861654Z，隔离恢复15项目43文件通过。随后仅增加3Resource与3ResourceVersion，验证所有原资源记录逐字段不变，见evidence/style-mentions/seed.json。启动本地5180/8010新版API与Worker。

## 边界

尚未执行本轮真实模型生成或新成片风格验收；不能以替身测试代替供应商效果。最终四主题视觉、用户完整流程仍待用户确认。当前项目的既有故事/分镜不因新增模板自动重写，须由用户选择模板后按正常流程使用。项目8848待确认且部分镜头可能超过所选模型时长，不替用户确认或缩短。
