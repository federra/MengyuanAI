# 项目、创意与故事交互反馈增量（2026-09-10）

基线0c3f796，分支codex/v12-v13-ui-alignment；实施目录为 /Users/nathan/Codex_Projects/MengyuanAI/.local/ui-v12-v13。以用户本次三条明确反馈覆盖旧交互，正式PRD04/06、R2/R4/R5及相关原型/标注同步；未重建项目或重跑M0～M3实施。

## 实现事实

1. 创建成功直接激活新项目并进入创意工作台；创建字段保留现有默认/校验与项目规格。
2. 创意移除常驻保存/读取基准/补充要求；700ms停止输入后自动保存，生成前仍确保保存。TXT入口支持非空UTF-8文件≤1MB，上传成功直接故事单卡，全文保存PostgreSQL；文件标题和原文摘录可编辑，不调用P26语义提炼模型。
3. 故事移除常驻保存修改/读取基准/单独确定故事/本次剧本生成要求；自动保存追加版本，生成前使用保存返回的版本，由现有后端原子选定并入队。故事质检位于右导演，更多方案位于左候选下；TXT单卡不显示更多方案。
4. 版本冲突与断网保留草稿、停止自动重试；冲突时显示服务端内容，由用户核对后才允许以自己的草稿追加新版。TXT未知结果冻结请求及幂等键，重试读取当前表，旧回执不把页面恢复为旧表。轮询串行化并使用请求序号，避免迟到响应回退内容。
5. 无schema迁移，TXT沿用内容/版本/配置绑定存储；旧候选和下游历史保留，导入或改文使下游过期。后续成功AI新批次恢复候选列表。超过50,000字符的原文完整保存，后续AI明确要求先确定改编范围，不静默截断。被移除的临时指令输入不再悄悄带入新任务，具名方法资源配置继续有效。

## 验证与边界

- 后端完整隔离回归214 passed，4条既有依赖弃用警告；包含7项TXT导入边界/幂等/跨项目/旧版本/下游失效测试。契约、构建、ruff通过。
- 页面完整51项回归通过（1.2分钟），包括慢轮询不重叠、迟到v1不能覆盖已保存v2；独立复审P2已修复验证。替身测试仅验证交互和请求，不充当供应商验收。
- scripts/story-flow-smoke.py/.mjs以真实Chrome→HTTP→FastAPI→临时PostgreSQL联测，无API替身：创建直达创意、创意及数量自动保存、8883字节TXT全文哈希一致、故事追加v2且v1保留、刷新恢复、另一项目空内容及跨项目404、零任务。临时数据库/服务finally清理；报告在evidence/story-flow/。
- 四主题受影响项目/创意/故事12张截图，原型与生产均分别记录；生产API替身截图保存在evidence/story-flow/themes/flow-*.png；原型副本在evidence/story-flow/prototype/。U01/U02/U03位置与公共样式核对，U04–U06工程/真实HTTP分别验证，U07新增真实Chrome设置页200%验证：innerWidth720、DPR2、visualViewport.scale1，无CSS缩放；创建/上传/正文/生成剧本/质检及导演收起恢复8控件可滚动到达，生成与质检仅trial未点击。长正文框需滚动，不能同时完整展示；报告native200-report.json与04–09截图。U08用户仍待验收。
- 本次新增模型调用0、费用0元；TXT内容来自隔离测试文件，未新增真实供应商或真实成片验收。M3既有真实MP4能力保留；用户独立全链及最终视觉仍待完成。

## 本地加载与回退准备

沿用用户已授权的本地服务切换范围。无需数据库迁移，先确认无活动任务，停止本项目应用进程、在原目录制作最新备份并隔离恢复，再从当前工作树用既有显式PYTHONPATH/媒体/凭据环境启动。数据库、Redis与PRD服务保持；两条历史unknown配音原样保留，不重发。原工作区未提交PRD/ZIP不修改。

本轮源码变更期间Vite可热更新，API/Worker已在最新备份验证后重启加载新接口；以下为完成事实。

## 用户复核步骤

1. 刷新5180，填写项目后点击创建，确认直接进入创意工作台。
2. 输入一句话，停止输入观察自动保存；或点击TXT上传，确认进入故事且全文与原文件相同。
3. 在故事中修改正文，等待保存后重载；查看质检在右侧、更多方案在左候选下（TXT不显示更多）。切四主题检查操作。
4. 本轮可只验不付费的创建/上传/编辑/重载；AI生成和质检仍是实际模型调用，按既有预算授权另执行。请反馈具体页面、主题与不符合点；工程通过不等于用户接受。

## 本地加载完成事实

最新停写备份：/Users/nathan/Codex_Projects/MengyuanAI/.local/backups/20260910T044224659268Z；12项目/36文件隔离恢复通过。停止与启动仅本项目应用监督进程及其API/Worker/dispatcher/Vite，未执行迁移，数据库/Redis/PRD服务保留。重载后全部业务表摘要与重载前一致，媒体哈希符合数据库；两条历史unknown任务保留，未新增任何任务或调用。新TXT路由已由运行中OpenAPI确认，12项目故事读取均成功。

既有M3真实MP4下载HTTP200，SHA256仍为74f339448e97dfedf532c42bd51831230b50d72cc011c3e1ad6b86840e6a8267；这只证明原成片保存可用，不冒充新成片验收。evidence/story-flow/local-reload.json记录数据摘要和接口验证，live-idea.png为真实5180只读核对。另在live-themes/补充1440×900真实项目/创意/故事四主题12张截图，与测试替身截图分开。

本地服务入口仍为http://127.0.0.1:5180/；当前源码/启动位置继续使用本工作树及v12-v13-local-upgrade.md的显式环境命令。原工作区及其PRD未提交记录保持。无合并、推送或公网部署；新增费用0元。最终用户视觉/交互接受与M3独立全链验收仍待用户判断。
