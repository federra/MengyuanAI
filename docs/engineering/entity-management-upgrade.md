# 三类元素管理本地升级与恢复方案

状态：代码及加法迁移已准备，尚未对本地正式库执行。本方案不包含公网发布、合并、推送或付费调用。

## 变更范围

工作树`.local/ui-v12-v13`，分支`codex/v12-v13-ui-alignment`，原检查点681550c。目标migration `20260910_entity_workflow`，来源`20260910_board_import`。新增实体归档标记、版本输入/输出图片与库来源字段、引用entity_id与可空图占位；新增entity_commands/library_assets/library_adoptions。已有记录不删除、不按同名归并、不替用户挑选历史图片；原字段值及文件保留。

## 预演证据

`scripts/entity-restore-rehearsal.py`从原有20260910T054447364699Z备份恢复临时库后升级：37张旧表原字段行摘要不变、36文件哈希不变，项目/分镜/导出/实体/引用/库列表接口可读。证据`evidence/entity-restore.json`。本预演没有启动Worker、没有复制凭据或改正式库，不能替代执行前的新备份。

## 执行顺序（待明确批准）

1. 检查正式库在途任务；若有在途/受理待核实任务，先保留并确认状态，不中断供应商任务。停止本地API/Worker/UI写入口，形成停写窗口。
2. 以工作树脚本、原正式媒体目录和原凭据目录执行`make backup`、`make restore-check`，保存新备份绝对路径；备份扫描已兼容旧schema，并新增library独立文件清单。恢复使用临时库/迁移兼容检查，不写回正式库。
3. 正式库运行`services/backend/.venv/bin/alembic -c services/backend/alembic.ini upgrade head`；以旧列投影摘要核对所有旧行与媒体哈希。随后从当前工作树重启本地API8010/Worker/UI5180（媒体、凭据目录沿用原目录，文本4096限制不变）。
4. 实际HTTP只读复核旧项目/版本/文件/MP4；用户在三个窗口做保存重载与视觉试用。新增测试数据若需要在正式库创建，明确标注为验收项目，不修改既有已验收项目。

## 恢复

加法迁移失败由PostgreSQL事务回滚；若切换后发现不可用，保持停写，将执行前最新备份恢复到新库并校验所有文件/行，再以681550c代码连接恢复库。不得直接运行会删除新业务表/字段的downgrade抹去切换后的用户修改。出现升级后新增数据时先单独备份，确认合并恢复方案后处理。已有凭据目录不覆盖、不输出。

## 试用入口与边界

升级后入口`http://127.0.0.1:5180/`，分镜→角色/场景/道具管理。正式升级前不宣称新后端在该入口可用。用户检查四主题中至少一个代表项目：切换元素保留草稿、统一保存重载、两类图片不混用、选择镜头添加/替换与阻删。暂不点击付费图片生成；接口/替身验证不等于本轮真实供应商或用户视觉验收。

## 执行结果

2026-09-10用户明确批准后已完成上述本地切换；新备份路径`.local/backups/20260910T082848960025Z`（本工作树内），13项目36文件恢复通过，正式迁移及旧字段/文件核对通过。新增一个明确标记的工程验收项目验证三类元素真实保存重载，项目数现14；费用0元。执行证据见`entity-management-verification.md`末节。前文待批准状态为方案形成时记录，当前本地服务已升级，不代表公网部署或用户视觉验收完成。
