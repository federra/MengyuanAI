# M2 最终故障恢复验收计划 · 2026-09-09

状态：用户授权继续完成 M2；开发分支 codex/m2-media-chain 已快进纳入 main 的部署记录。用户已明确回复“批准”，新增付费演练已完成并通过独立复核，结果见 [最终验收](m2-final-verification.md)。仅在本地验收，不改变公网服务，不进入 M3。

## 验收目标与既有事实

R5 M2 退出条件与 A04 要求中镜失败后第三镜等待、恢复不重做成功第一镜。真实三镜、参考图/TTS、尾帧链、受理后重启、保存重载、失效传播、隔离和停写恢复已通过；此次补做专项故障恢复，不能用替身生成结果替代供应商结果。

项目为 8916ef02-2715-468f-86fa-c5dfc84673a8「M2 真实媒体验收 · 卡通小动物送果子 · 三镜」，分镜 revision 5，三镜各4秒，最多两句已有配音。只读预检：API健康200、视频凭据configured=true、项目无活动任务。保留旧结果与历史，不读取或输出密钥。

## 最小执行方案

复用7张已确认卡通参考图和2段真实配音；通过现有顺序生成接口重新生成3镜，各4秒/720P/16:9。当前顺序接口要求从第一镜起连续提交，因此此次演练需重新建立完整三镜链；故障后的恢复必须保持本轮第一镜任务、结果和文件完全不变。

仅在本地媒体Worker的进程PATH中加入一次性ffmpeg包装器，精确匹配本轮第二镜job对应staging路径及尾帧提取参数；原视频检查和其他任务透传真实ffmpeg，目标尾帧提取一次返回非零，随后自动失效。不修改供应商响应、不伪造视频，不添加生产后门、不直接修改任务状态。启用前用本地无网络夹具确认匹配范围，停写并核对没有活动任务后短暂重启本地Worker环境；结束恢复正常PATH。

预期第二镜真实视频已生成并保存staging收据，尾帧失败得到last_frame_extraction_failed；第三镜仍waiting_dependency且无external_id。通过现有重试接口恢复，优先复用staged_key和原external_id，不创建第二次供应商生成；第三镜依赖重试任务并在成功后继续真实生成。不将本地注入故障描述为供应商拒绝。

## 用量与止损

新增上限：3次视频创建、请求总时长12秒；0次生图、0次TTS、0次TOS或素材库调用。复用用户此前确认的Mini四折费率与上一轮usage，估算约2.41元，预留3元，硬上限5元；金额以供应商账单为准。官方当前页面仍列Mini无视频输入23元/百万tokens；四折以用户已确认账号权益为依据，不将官方刊例价误认为活动实付价。

最多1次本地失败恢复操作，只允许复用已持久化真实文件/原外部ID；若需要新建付费重试、费率变化、未知受理或无法在上限内完成，立即停后续提交并报告，不自动增加次数。此次方案已获用户确认；创建上限仍为3次，不增加额度。

## 证据及收尾

保存本轮三job/sequence和重试job、外部ID、事件流、冻结配置、引用与尾帧关系；失败时保存第一镜成功和第三镜未提交证据；恢复前后比较第一镜ID/文件SHA256不变，第二镜原external_id/staged_key复用且保留失败历史，第三镜previous指向恢复后的第二镜结果。浏览器检查失败提示、重试入口可达、恢复后3镜播放及刷新保留。恢复正常Worker后停写备份并隔离恢复核对；按变更范围做回归和独立复审，更新M2完成状态及验证边界。未获本轮新授权不推送、合并、部署或进入M3。
