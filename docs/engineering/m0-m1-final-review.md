# Whole-branch final review — cf2d2c6..d590ef0

**Historical review: the two P2 findings below were fixed in66f64af and approved by the final scoped re-review (m0-m1-final-rereview.md).**

**Original verdict: Changes requested: two P2 recovery defects.** Scope: current M0 configuration and M1 content/UI plan, with real credentials explicitly absent. Reviewed final package, plan/progress, all three task reviews/re-reviews, verification record, current PRD 04–08 and R4/R5 contracts; inspected configuration/content execution, lineage, retry and affected frontend paths in bounded passes. No native data, credentials, source, index or branch state changed; no full suites rerun.

## Strengths

Append-only resources/content and locked project transactions provide a coherent configuration-to-task contract. Late stage generations are archived, adoption validates exact source versions, quality findings remain advisory, and board identities/reference ownership are checked server-side. Prior narrow review fixes are addressed. Reported 49 backend/15 browser checks, real HTTP/PG/Redis Worker fixture chains and native upgrade/restore evidence have clear boundaries; none claims real DeepSeek quality or M2/M3 delivery.

## Important findings

1. **[P2] Retire definitively rejected stage generation commands so conflicts can recover.** `apps/web/src/StageWorkbench.tsx:23–40` always reuses the stored pending body, but deletes it only after successful POST. If the target advances between GET and POST, the server returns 409 before creating a job (`services/backend/src/shortfilm/creation/stages.py:107–113`). Every later attempt reads the newer head and then discards it in favor of the old cached `target_revision`; reloading retains the same failure. The UI has no way to abandon this definitively rejected command. Execute actual transpiled `generateStage` and `commands.ts` against isolated memory storage/API: GET initially returns revision 1, POST advances the fixture to 2 and returns 409; three calls submit target revisions **[1, 1, 1]**, and both pending storage records remain. Handle a definitive pre-enqueue 4xx rejection separately from a lost/unknown response: allow a fresh validated command and target revision after rejection, while preserving the original body/key for uncertain outcomes. Add a focused conflict→retry regression and retain the existing lost-response/reload assertion.

2. **[P2] Provide a source-consistent latest-version recovery for regenerated stages.** `apps/web/src/StageWorkbench.tsx:485–486` refreshes only the draft base revision; the history “载入为草稿” handler at `:543–546` replaces body/base but never source. Draft source is initialized from local cache (`:234–239`) and sent unchanged on save (`:469–471`). Repro path: visit script v1 sourced from story v1 (draft is automatically cached); revise story and regenerate script v2 from story v2; reopen script; click “读取最新基准”, load v2 from history, edit and save. The request still sends `source_version_id=story-v1` with revision 2, so the backend correctly rejects the obsolete source (`creation/stages.py:62–67`). Reload repeats the cached mismatch; a normal user cannot save edits to the regenerated content through the provided recovery controls. Add an explicit, draft-preserving way to load/rebase to the latest current content with its exact cross-stage source. Keep manual-save source validation: do not merely relax the backend or silently relabel a historical board; its `body.scriptId` must stay consistent with the chosen current source, and historical versions from another source need an explicit supported handling path. Add script and board regeneration→latest recovery→save regressions. Source/control-flow evidence is conclusive; an attempted fully intercepted Playwright reproduction could not launch Chrome in the sandbox (SIGABRT), so no browser-pass claim is made for this review.

## Assessment

**Ready to merge: With fixes.** The existing independently approved task fixes remain approved; these two integration recovery cases are outside their existing successful-path/unknown-response coverage. No critical security or data-loss issue found in the inspected scope. No stylistic refactor, deferred media/TXT work, paid-model call or additional review seat is requested. After the two focused fixes, re-review only their diff and regressions, preserving the original uncertain-response idempotency guarantee.
