# M0/M1 最新计划工程验收 · 2026-09-08

状态：M0 配置增量与 M1 内容工程已经实施，三项开发任务均通过独立审查；最终整分支复审发现两项P2恢复缺陷，尚未修复；用户要求关机，已暂停于安全检查点，不能声明本轮全部收尾。用户明确确认 DEEPSEEK_API_KEY 尚未注入，本轮先完成工程与替身验收；不宣称真实文本模型或 M2/M3 成片验收通过。

## 基线与审查

原 M0 标签 `m0-baseline-20260908`（cf2d2c6）保留。故事基线 b8d0c82 增量前再次验证：22项后端、6项浏览器、契约/构建通过。用户更新的 AGENTS/PRD/Demo 原地保留，不混入工程提交。

配置增量 8ea8eed、修复 9a93433：资源与绑定不可变版本、类别默认/环节覆盖、项目规格、具名方法、模板预览及 unknown 备份。独立审查发现并修复风格双来源不同步、视频字段接受非视频能力；33项测试通过，复审 Approved。

内容主链 190657b、修复 7f330b0：剧本/分镜版本与确认、非阻断质检/修复、创意及各阶段导演建议、稳定镜头/台词 ID、冻结配置和来源、迟到归档、共享 AI 队列及恢复。独立审查发现并修复剧本原样重存插入标题导致下游过期；48项测试通过，复审 Approved；其后为原故事查看增加受项目/owner隔离的精确历史版本查询，后端现为49项通过。

必要调整：停写后允许原样备份历史 unknown 并列出待核实任务，恢复不自动重投；R4 补充 P29 storyReview（最新 PRD 要求故事质检而原清单遗漏），不重编号既有 P01–P28。未重写 Demo/阅读器、存储或任务架构。

## 工作台与配置验收

前端 c2da959、修复5924907：五模块、五阶段、具名资源配置、生成附加要求、四阶段助手固定/收起/悬浮、剧本精确原故事折叠查看、助手内建议性质检、可编辑分镜表及失败恢复已接入生产API。独立审查的5项问题全部修复并复审Approved，其中固定旧版模板的查看/保存不会静默升级，必须显式升级资源版本。

最终 `make test-ui` **15/15**，`make test` **49/49**、OpenAPI精确匹配、TypeScript/Vite构建及Ruff通过。浏览器替身回归覆盖冲突草稿、重载幂等、冻结生成要求、方法保存竞态/项目隔离、稳定镜头/台词ID、助手模式/报告位置、精确来源和模板固定版本。真实隔离API页面检查零pageerror，三主题分镜、模型文字树及剧本原故事截图已目视检查。界面替身仅证明交互，真实HTTP替身链另列如下。

## 真实进程与本机 HTTP 文本替身集成

独立 PostgreSQL 数据库、真实 FastAPI HTTP、Redis、dispatcher、Celery Worker；供应商端为本机 HTTP Chat Completions 替身，实际通过现有 httpx 适配器，不直接调用 execute_job 或绕过传输层。队列使用本次专用 Redis 前缀，与本地项目隔离。

已实测：创建16:9/720P项目 → 保存创意 → 恰好3故事/重复命令同任务 → 导演建议不改正文/采用追加版本/旧版保留 → 生成剧本及质检 → 人工修改/报告/修复建议采用 → 生成分镜 → 重排镜头、追加台词保留ID → 重复ID拒绝 → 修复采用/保留当前分镜确认 → 原样保存剧本保持分镜有效 → 供应商503产生unknown、未确认重试409、显式确认后成功 → 重启API仍读到精确分镜及确认。追加真实PNG上传、合法图片引用、原文件HTTP读取及分镜重新确认也通过。

结果：19次本机模型HTTP调用，全部携带项目规格；所有上述断言通过。界面修复后再次从零隔离回归18次HTTP调用通过，并实测新增精确来源版本查询。两轮记录分别为 `.local/m1-integration/report-before-ui-fixes.json`、`.local/m1-integration/report.json`，执行脚本 `.local/m1-integration/run.py` 为本次隔离验收工具，不是生产模型或内置演示后门。替身正文明确用于工程测试，不能证明真实模型的语言、质量、成本或可用性。

## 数据保护与验证边界

升级前 native 备份 `.local/backups/20260908T104642732412Z`；独立恢复演练通过，2项目/1媒体文件。原项目库现已升级至 `20260908_m1_content`，2项目/1媒体/1内容及版本/1历史任务保留；升级后备份 `.local/backups/20260908T120606312297Z` 再次隔离恢复通过（2项目/1文件）。各轮 `make test` 使用临时数据库，包含迁移往返、真实Redis/Worker中断恢复及备份恢复；契约精确比较、TypeScript/Vite和Ruff均通过。两条既有 Starlette/httpx/anyio 弃用警告未隐藏。

原生 `make dev` 已启动API、AI/媒体Worker、dispatcher与5180页面；实际HTTP再次读取2项目及媒体，SHA256一致，原创意版本保持；未注入密钥的生成返回422“文本模型凭据尚未配置”，没有创建任务。记录 `.local/m1-integration/native-report.json`。

PRD manifest 校验23模块/11页面/68关系；V8/V9/V10规则测试通过。这些属于文档/原型验证，不代替生产验收。Docker 本机未安装，容器启动未验证；真实DeepSeek、M2图像/TTS/视频、M3 MP4及后续TXT入口未计入本轮完成。
