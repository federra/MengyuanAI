# M2 实施与验收事实

2026-09-09：开发分支codex/m2-media-chain；main/GitHub同步且初始工作区干净，回退标签m1-baseline-20260908指向667481a。已阅读最新范围及UI边界，实施计划见m2-implementation-plan.md。

基线make test通过：64后端测试、OpenAPI一致性、前端构建。首次沙箱连接本机PostgreSQL被阻止，获得工具执行批准后在隔离数据库运行成功；不是工程失败。

尚未完成M2工程及真实媒体验收。用户要求推荐媒体供应商，正在核对官方能力；没有执行付费媒体请求，没有读取或输出凭据。生产数据库尚未迁移。UI视觉验收仍未通过，不改变M1真实文本已验收事实。

## 元素版本增量

新增角色/场景/道具稳定ID和追加版本、API历史列表、revision冲突、类型不可改变、项目归属校验。先观察新增2项行为测试因接口404失败，再实现后66项后端测试通过；make check、OpenAPI与前端构建通过。Alembic在隔离空库执行升级→降级→升级；未迁移生产库。尚未接入图片/台词绑定、前端管理与影响传播，因此本项仅为元素版本基础。

官方Seedance创建任务文档已核实：首帧、首尾帧、全模态参考三种模式互斥；多图中只能用提示词间接指定首帧。已向用户说明并询问接受尾帧参考还是要求严格首帧组合，未擅自弱化R5能力门槛。

参考图增量正在实现：原始上传文件不改写，新增元素来源版本和人工确认记录，确认绑定项目规格revision。新增2项测试先因接口不存在失败。FFmpeg/ffprobe尚未安装完成时媒体处理3项测试失败，不能计入通过；Homebrew安装仍在下载。生产库仍未升级。

## 当前已验证结果

- 元素版本及参考确认API共新增5项行为测试；全量72后端测试通过（含3项媒体工具测试）、make check、OpenAPI契约、前端构建通过。规格变更需要重新确认；改名不使图失效，外观描述变化使旧图过期；原文件仍可读取。
- 新增角色/场景/道具管理弹窗及参考图上传/预览/确认；24项浏览器回归通过，新增2项覆盖弹窗关闭/重开、409草稿保留、Escape焦点，以及按精确文件ID预览和按规格revision确认。UI测试为独立5181及显式API替身，不是生产/供应商验收。
- FFmpeg9.0.1安装完成，3项本地夹具验证通过。此前安装未结束的缺依赖失败已解决。没有依此宣称真实模型媒体通过。

未完成：参考图AI生成、站位图、镜头/元素和逐lineId音色绑定、TTS/视频适配、媒体任务快照及对账、顺序依赖/暂停继续/中镜失败恢复、源媒体影响传播、生产迁移与媒体恢复、真实项目验收。供应商组合及多图/严格首帧取舍等待用户决定，费用批准尚未请求执行。独立审查进行中。

## 独立审查修复与本次检查点

独立审查发现3项P2：新建/仅描述草稿恢复入口缺失，恢复冲突草稿会恢复旧revision，尾帧按容器时长定位会错过较短视频流。新增2项浏览器行为测试和1项真实本地FFmpeg夹具测试，均先观察失败再修复。草稿改为项目内多份备份，恢复后展示当前保存内容并使用当前revision，仍须用户点击保存；尾帧顺序解码视频并覆盖单张输出，不受长音轨影响。

最终make test通过73项后端测试、契约一致性和前端构建；make test-ui通过26项；make check及git diff --check通过。独立复审关闭原3项问题，并独立重跑长音轨样本成功，未发现新增阻断项。保留2项既有依赖弃用警告。

这是M2部分工程检查点，不是M2完成。全部新界面仅在独立测试服务验证；生产库没有升级、生产服务没有重启；真实供应商调用为0，真实媒体结果为0。分支未提交、未推送、未合并；没有进入M3。后续仍按上文未完成清单推进，供应商组合及视频约束等待用户答复，正式付费方案须另行批准。


## 供应商接受后：媒体链工程检查点（2026-09-09）

本节为最新事实，前文“未实现/待选型”是各子阶段历史状态。用户接受 Seedream + MiniMax speech-2.8-hd + Seedance 组合，以及真实前镜尾帧作为多参考图输入的语义；提供视频模型 `doubao-seedance-2-0-mini-260615`，已填入配置草稿预设，仅支持本流程720P，未自动修改生产配置。

已实现：真实接口适配器、图像/音频字节和视频异步ID处理、下载与解码入库、配置及来源快照、逐lineId角色音色与独立表现参数、引用替换保留稳定ID和原文件、人工图片确认、顺序链真实末帧、持久依赖/暂停/继续/失败重试、结果历史与过期传播、跨项目校验。未知受理不自动重新付费；已知外部ID重试查询原任务。没有将媒体夹具写入生产项目。

最终自动验证：`make test` **101 passed**，23.55秒，包含独立临时PG升级/降级/重升级、真实Redis/Worker恢复，OpenAPI一致性和TypeScript/Vite构建通过；`make test-ui` **28 passed**，35.4秒，独立5181及显式API替身；`make check`、`git diff --check`通过；PRD validate为23模块/11页面/68关系。保留2项既有Starlette/httpx/anyio弃用警告。浏览器截图 `apps/web/test-results/m2-media-workbench.png`仅为替身工作台，媒体URL故意不提供真实生成文件，不能作为生成结果展示。

独立审查已关闭：取消后重试丢失顺序依赖、付费成功但落盘失败误判可安全重试、晚到旧结果覆盖新请求、Mini/fast不支持规格未前检、同文件历史绑定遮蔽新确认、未知受理确认跨任务共享；下载地址校验及实际IP固定防止地址重绑定。新增行为测试覆盖同文件新版本重新确认与API结果排序。只读复审未发现未关闭问题，不替代真实供应商验收。

Worker证据：本地HTTP供应商替身 + 独立真实Celery/Redis；在已有外部ID的查询中SIGKILL，主动令租约过期后重启，断言POST仅1次、GET2次。不代表已验证真实供应商在ID持久化前崩溃、真实下载入库或真实三镜连续性；这些仍待付费验收。

生产检查：只读核对7项目、25内容版本、2媒体文件、任务33成功/3失败，无活动任务；当前仍为M1数据库版本 `20260908_m1_content`。停止应用/Worker→备份→恢复核对→升级数据库的命令被**自动审批审查拒绝**，理由为未明确授权停写或生产迁移，命令未执行。未停服务、未迁移、未创建此次迁移前备份或快照。需用户明确批准后再执行具体升级流程，并核对既有内容/文件哈希及读取。不得以分支测试代替生产保全验收。

剩余：生产停写备份/隔离恢复/迁移/重启；页面录入媒体密钥；核算并批准付费最小方案；真实项目图片/TTS/三镜视频生成、保存重载、失败重试/中断恢复/上游失效/项目隔离证据。真实媒体调用0、真实媒体结果0，M2未完成。未进入M3、未提交/推送/合并。UI仍未通过用户视觉验收。


## 已批准的本地生产升级及测试污染更正

用户明确批准“暂停应用/Worker、备份、隔离恢复、迁移、重启及M1保留核对”。已完成：停止准确核对的本项目进程；备份 `.local/backups/20260908T214251968797Z`，校验2文件；在独立数据库和新媒体路径恢复，7项目/2文件检查通过；Alembic升级至 `20260909_m2_ref_versions`；迁移前后projects/content_versions/media_files/generation_jobs全行排序哈希逐表一致（7/25/2/36），文件SHA256一致；随后make dev重启。HTTP健康及全部7项目的分镜、元素、参考图、媒体结果/任务读取通过，5180页面可访问。页面已保存三类模型默认配置v1，密钥均尚未配置；未发起连接或生成请求。

**更正此前“没有写入生产项目”的错误表述**：页面核查发现3个“媒体进程恢复验收”。负责定向测试的子代理确认，05:27:04、05:27:41、05:28:27三次直接pytest未指定隔离库，创建项目后因缺M2表失败。只读核对：项目6d737ef1-4f99-48a2-97ef-11dc7dfc0fae附带1张测试图片；d9c91d53-b941-4356-97b2-19cc51c6e348、92c88117-fe5c-4928-aaa8-8f1a20a9c534为空；三者均无内容版本、生成任务或元素。最终通过的Worker测试确为隔离库，但不能覆盖此前误写事实。这3项目及测试图不是用户作品或供应商结果，暂保留并已如实告知用户，未擅自删除。迁移哈希一致只证明升级没有进一步改变记录，不代表此前没有测试污染。

补救：tests/conftest.py在pytest会话启动、测试模块导入前强制检查显式 shortfilm_test_* 数据库和独立系统临时媒体子目录；缺失则退出4，不连接数据库。直接运行旧命令的collect-only已验证拒绝。全部测试必须从make test进入。真实供应商调用及结果仍为0。

隔离保护补齐后的复测：make test再次101 passed（21.43秒），契约/构建及make check、git diff --check通过。生产界面模型树已逐项确认三类默认配置v1保存成功、尚未配置媒体密钥；生文配置未修改。

隔离保护复审另发现URL查询参数可覆盖路径库名：已在pytest入口拒绝query/fragment及非psycopg地址，并在scripts/test.py任何连接/迁移之前拒绝query。带dbname覆盖的两种入口均已验证提前退出，不连接数据库。

最终复核：URL覆盖保护P1经独立只读复审关闭；更新后的pytest入口全量101 passed（21.58秒），契约/构建通过，启动器拒绝覆盖参数的分支另行负向验证通过。

用户已明确批准真实验收上限：30元、图像10张、TTS10次且1000字符、视频5次且25秒。当前状态接口三类凭据均configured=false，尚未调用。官方核对MiniMax地址为https://api.minimaxi.com/v1，修正前端预设及系统audio绑定至该地址；没有读取/转移密钥。用户请求页面配置指引。

音频密钥按钮排查：状态接口显示image/video已保存凭据（revision4），audio未保存；仅查看非密钥DOM确认用户音频草稿仍是旧https://api.minimax.cn/v1，服务器为https://api.minimaxi.com/v1，dirty保护禁用保存。未读取输入框密钥。尝试修正地址及同步基准被自动审批拒绝（地址变化会清空未保存密钥，用户尚未明确授权该状态丢失），操作未执行；交由用户改地址后重新输入。原“读取最新配置基准”只更新revision并保留草稿，不会替换旧地址，先前指引遗漏此点。尚未开始收费请求。

MiniMax地址纠错（用户指出后重新核实）：https://platform.minimax.cn/docs/api-reference/speech-t2a-http 当前明确使用https://api.minimax.cn/v1/t2a_v2；此前只读取旧域名文档便否定.cn的判断错误，已向用户承认并恢复预设.cn。不能再要求用户一律改成minimaxi.com。模型保存与凭据状态成功不等于接口地址适配/供应商鉴权通过；状态核对时用户实际配置曾为api.minimax.com，须区分三个不同域名。

## 2026-09-09 充值后真实配音检查点

真实Seedream累计8张成功、7张确认；MiniMax累计6次请求（73输入字符）得到4段真实MP3，两次失败的重试均经用户明确批准。四段HTTP/文件SHA256、跨项目404、独立浏览器解码与重载通过；真实修改第一句仅对应音频过期，恢复源文本后有效，版本历史保留。详见[m2-real-verification.md](m2-real-verification.md)，不再采用此前“等待音频充值”的状态。

视频首镜重试仍为HTTP400且无外部ID，后续两镜保持等待依赖；未盲目消耗最后一次视频额度。当前仅补齐固定错误类别识别，支持官方InvalidParameter命名空间，不记录响应正文/密钥；该工程缺口不是已确定的供应商拒绝原因。最终媒体备份仍待在途顺序任务完成或对账后停写执行，不能拿升级前备份当作新媒体恢复证据。


## 2026-09-09 素材权限与传输层检查点

用户已在页面保存AK/SK v1；批准的真实读取检查确认TOS桶和方舟素材组访问成功（1次HeadBucket、2次ListAssetGroups含首次缺参失败，总批准上限1元）。新增安全配置、读取检查、独立上传/审核传输层；完整142项后端测试、契约/构建和静态检查通过。29项页面回归后另针对配置刷新修复复测1项通过。尚未整合持久素材绑定与顺序视频入库，未发生真实上传/素材创建/新增视频，M2仍未通过。详情见[m2-ark-assets-integration.md](m2-ark-assets-integration.md)。
