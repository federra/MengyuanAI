# M1 真实文本验收 · 2026-09-08

状态：M1退出条件通过——真实文本模型走通到确认分镜，非法输出/过期建议被拒，人工修改和版本保存、失败恢复通过。用户已在生产页面保存DeepSeek密钥并报告连接成功；代理仅读取配置元数据，未读取凭据文件、密码DOM或.env。

## 验收环境与证据

生产应用5180、FastAPI8010、PostgreSQL55432与Redis56379，真实Celery Worker使用页面保存的凭据。独立项目「M1 真实验收 · DeepSeek」，ID `9c975b3c-044f-41b2-8e7d-0173e6c21dbc`，16:9/720P。实际任务快照：provider deepseek，model deepseek-v4-pro，官方API地址，JSON对象模式，thinking disabled；配置由用户页面保存。原有两个项目保留。

生产写操作全部经过应用API，未通过SQL拼接或修改内容。只读JobAttempt的耗时、用量与校验诊断用于验收。过程命令及幂等键保存在忽略目录 `.local/m1-real-acceptance/`，供应商输出与API状态保留为本地证据，不纳入Git。

## 已执行内容与边界

- 一句话生成三份不同故事；选择《父亲的信》，导演建议未直接覆盖原文，明确采用再人工修订，故事保留3个版本。原始内容仍可读取。
- 真实剧本生成、建议质检、导演修改、人工保存。人工保存后尝试采用旧建议返回409，当前版本未改变。
- 对当前剧本重新质检、生成修复建议、明确采用，并人工调整分场，保留4个剧本版本。修复建议并非无瑕疵：动作与台词时序仍需人工判断，未将供应商的“已解决”描述当作质量证明。
- 首次分镜生成三轮契约校验未通过，标记failed；显式重试保留retry_of和原配置，仍失败，没有发布非法分镜或破坏已确认剧本。
- 发现纠错反馈只包含Pydantic类型，遗漏自定义校验具体原因。必要修复保留不含input/context的错误诊断，反馈具体原因，并把台词一致性校验定位到Shot以给出镜头路径。拒绝条件不放宽；诊断不是通用脱敏器，错误消息可能包含非法字符片段，不能当作凭据存储通道。
- 修复经红绿回归（先缺少具体原因失败，再缺少shots/index路径失败）及独立Approved审查；64后端测试、契约/构建/静态检查通过。本轮21页面替身回归通过。真实重试诊断确认：模型曾将人物/场景名称写入图片UUID引用；改正后仍有台词拼接不一致。首轮旧代码未保存详细原因，不能倒推所有历史失败的具体错误。
- 两次停止无在途任务的开发服务后重启；页面凭据配置仍可用，故事/剧本版本保持。unknown中断与非法JSON等故障由隔离回归覆盖，真实供应商未人为制造网络断连。

## 最终结果

明确空引用与台词拼接要求后的任务 `2055db3d-180e-4245-9724-075e0cfc150d` 成功生成11镜，15条台词记录（含3条无对白占位）。人工修改动作时序及镜头秒数，追加分镜v2，原有镜头/台词ID保持；镜头时长合计126秒，是分镜计划值，不是真实视频时长。剧本全部12条非空台词保留。

通过生产页面“保存分镜 → 保留当前版继续”完成确认。当前版质检成功返回，建议可保留不采用；确认版本 `799df453-e2da-4cb4-8200-56fbc65676ac`，确认ID `868e0e95-2b70-4261-a2d5-abb398bd0768`；来源剧本v4 `cd264c66-05c5-4edb-9514-e7fab9729722`。新浏览器和服务重启后均读到v2已确认，无页面错误。

项目共17个真实任务：15成功、2失败；23次供应商调用（包含有界纠错与自动质检），供应商usage合计141459 total_tokens，包含输入与输出；不含用户自行连接测试，不代表货币账单。完整快照/数字用量/错误路径在 `.local/m1-real-acceptance/evidence.json`。工程修复提交 `9437b3e`，独立复审记录 [纠错独立审查](m1-correction-review.md)。

停写备份 `.local/backups/20260908T141754855062Z` 已通过校验；恢复到隔离PostgreSQL和新媒体目录，3个项目/1个媒体可读；额外通过应用API核对真实故事3版、剧本4版、分镜2版、11镜和确认ID与原库完全一致。恢复演练未调用模型、未恢复或读取凭据；生产服务已重新开启。

M1通过不代表默认提示词每次均成功，也不代表AI故事或质检建议可免审。此次两次分镜失败及人工纠正均保留，未降低契约门槛。公网安全、容器启动、M2图片/配音/视频和M3真实MP4不属于此次通过声明。
