# V8 工作流验收记录

验证日期：2026-09-08。范围为独立交互 PRD 与原型，不含生产 M0/M1 工程或真实供应商服务。

| 检查 | 结果与证据 |
|---|---|
| 文档与索引 | `npm run validate` 通过：23模块、11页面、68关系；`git diff --check` 通过 |
| 原有状态回归 | `workbenches-v5.mjs` 通过：TXT、稳定台词ID、报告结构及原子修复、TTS、视频任务快照和项目隔离 |
| V8 状态回归 | `workbenches-v8.mjs` 通过：共用方法、规格继承、尾帧幂等与来源过期；AI建议/无报告时继续，原报告不变；空内容/未知引用拒绝 |
| 实际点击主链 | 隔离Chrome，1600×1050：创建16:9/4K项目→选择写作方法→3故事→选择剧本方法→问题剧本直接继续→问题分镜直接确认→参考图→第一镜视频→第二镜取前镜尾帧并预览→导出继承4K；页面无JS错误 |
| 分镜二次生成 | 助手选择方法后刷新整表，boardVersion递增且旧表归档；无自动质检或设置弹窗；可选修复仍正常，助手可收起 |
| 视觉检查 | 检查深色项目/剧本及晴空蓝分镜；修复方法编辑入口遮挡主按钮的问题；分镜四类参考窗纵排、尾帧角标与点击预览可操作 |
| 阅读器与标注 | 7个相关页面的20个活动标注锚点均存在；修复组件页旧主题锚点；阅读器R2能显示V8内容 |
| 技术流程图 | 项目、创作、媒体链三张Mermaid源文件重新生成SVG，均无语法错误 |

浏览器回归命令：在PRD目录，安装了Playwright时执行 `node reference/tests/workbenches-v8-browser.mjs`；可通过 `PLAYWRIGHT_MODULE` 指定现有模块绝对路径、`CHROME_PATH` 指定Chrome、`PRD_URL` 指定本地原型地址。测试使用独立浏览器上下文，不覆盖用户浏览器中的项目。

边界：AI文本、报告、图片与视频仍为示例；本轮尾帧主链验收使用明确标记的演示尾帧，未将其作为真实视频截图或真实MP4验收。生产规格迁移、数据库review_decisions、供应商调用与异步任务由R5增量计划约束，尚未随本次原型修改实现。
