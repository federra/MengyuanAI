# v3 原型验证记录

日期：2026-09-07。对象为 R2 前端交互原型；未调用外部模型或收费服务。

- 浏览器主流程：一句话生成三个不同小说选题；详情全文、AI 修改并保留历史、选择剧本提示词、选定生成剧本、自动质检、按报告修复、确认进入分镜。额外要求生成第四个选题已验证。
- 小说入口：输入三段故事，选择 30 秒和 16:9，直接生成对应分场剧本并自动质检/修复。恢复 60 秒规格后出现重新改编提示，不能直接确认旧剧本。
- 分镜：生成后自动质检，采用建议并复检；四类参考图生成与确认；点击 @站位图打开对应预览；新增第四镜、确认删除后恢复三镜。
- 音频与视频：单镜 TTS 示例独立完成；配置中心音频模型页默认为 audio 能力并可保存；三镜顺序生成成功，后镜有前镜尾帧预览；模拟失败暂停后续，继续生成后完成。
- 代码断言：30/60/90 秒小说字数上限及三方向差异；修复带报告并保留人工追加文本；来源/规格确认门禁；过期 JSON 报告原子拒绝；改名保持 ID 引用，未知引用阻止生成；无尾帧不能顺序提交；前镜变化使后镜视频/尾帧待更新。全部通过。
- 视觉：1440×900 创作页及分镜浅/深主题走查；正文 15px，配置收拢，表格局部滚动。14 条现有标注逐条点击确认对应气泡选中，新页面锚点按实际 DOM 测量；6 页画布自动排列，卡片不重叠，关系箭头可见。
- 边界：TTS、视频及生成视频的尾帧为示例，浏览器朗读不等于生成音频。真实上传视频末帧提取有前端实现，但本轮未验证编码格式兼容性；真实供应商调用、长小说拆分、音画合成、MP4 导出和持续后台恢复均未验证。未宣称这些能力已接通。

运行 npm run validate；最终结果为 21 个模块、6 个页面、16 条关系。没有修改 runtime。历史原型保存在 reference/history/before-novel-flows/。

## 视觉精修回归

2026-09-07：按 gpt-taste 精修 6 页共享视觉，增加本地 GSAP 3.15.0 / ScrollTrigger 与装饰 SVG，未修改存储、小说/剧本/分镜/成片业务模块。固定 1440×900 检查浅深创作页、小说详情、剧本、分镜、配置及成片页；浏览器检查无 JavaScript error。14 个锚点重新测量并逐条点击，均定位到对应选中气泡。语法检查与 manifest 校验通过（21 模块、6 页、16 关系）。此前版本保存在 before-taste-polish；runtime 未改动。此轮为视觉与原有入口走查，不替代真实模型或完整无障碍测试。
