# 2026-09-08 工作台与导演助手回归

范围：项目封面、状态与筛选；五阶段统一标题；导演助手固定/收起/悬浮与建议采用；TXT单卡；分镜设置、全片元素管理、多段台词、逐段配音；同步PRD与28项提示词清单。旧版位于history/before-director-workbenches。

## 已验证

- Node：`node reference/tests/workbenches-v5.mjs` 通过。覆盖新项目画幅为空、TXT单卡/完整原文/旧候选归档/空输入拒绝、画幅确认前剧本质检、JSON v2镜头与台词ID及字段校验、修复保留多段台词、所选视频模型快照、逐段TTS与音色变化失效、连续尾帧依赖、项目隔离。
- 浏览器1440×900：创意建议→采用；助手收起后主栏由865px扩大至1113px；生成三故事→选定→剧本自动质检→修复复检→确认并进入分镜设置→16:9分镜→自动质检→采纳复检→确认。首镜两角色两段台词完整保留；批量TTS逐段显示结果。
- 角色集中改名与音色保存后，台词名称同步、音频标记待更新；场景和道具管理弹窗、批量图片示例可用。修正隐藏文件输入撑宽弹窗问题。系统设置可打开比例/分辨率/模型与计算尺寸对话框。
- 项目卡显示创作中与完整时间；只看已完成正确显示空结果，未完成筛选与最早编辑排序组合正确。创建表单无画幅。无短片素材显示空封面，有素材时使用本项目画面或用户上传封面。
- 浅色创意/故事/项目/管理弹窗与深色分镜已截图检查；菜单、流程16px，正文15px，分镜正文14px。新增组件展示页可操作建议、采用、收起；状态页补充TXT单卡、未设置画幅、配音失效和过期建议。
- PRD底座R2可进入新原型。项目、创意、故事、剧本、分镜本轮相关标注逐条点击检查选中气泡。新锚点根据固定视口DOM实测维护。画布已通过底座自动排列并保存，11个节点的DOM矩形不重叠，68条关系保留并更新名称。

## 验证边界

TXT原文与单卡转换已做数据级测试，本轮未通过操作系统文件选择器重复实测上传；真实AI、图片、TTS、视频与编码服务未接入，不宣称生产生成成功。原型生成图片与尾帧均标示示例。正式文件导出能力沿用上一版，本轮未重新执行全量导出测试。
